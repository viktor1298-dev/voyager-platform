---
phase: 03-validation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03]

must_haves:
  truths:
    - "pnpm build exits with code 0 (all 6 workspace packages compile)"
    - "pnpm typecheck exits with code 0 (strict TypeScript checking passes for all packages)"
    - "pnpm test exits with code 0 (all 128 Vitest tests pass, including 3 PostgreSQL integration tests)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL 17 + TimescaleDB + Redis 7 for integration tests"
  key_links:
    - from: "pnpm test"
      to: "docker compose (PostgreSQL)"
      via: "DATABASE_URL env var connecting to localhost:5432"
      pattern: "health-check\\.integration\\.test"
---

<objective>
Run the full validation gate (build, typecheck, test) against the merged codebase with Docker infrastructure running, confirming all 128 tests pass including the 3 PostgreSQL-dependent integration tests that failed in Phase 2 due to missing Docker.

Purpose: Provide independent verification that the Phase 2 merge produced a fully working codebase, closing the validation gate before Phase 4 (push to origin).
Output: All three validation commands exit 0. Phase 3 requirements (VALID-01, VALID-02, VALID-03) satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-validation-gate/03-CONTEXT.md
@.planning/phases/02-the-big-merge/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start Docker infrastructure and run full validation gate</name>
  <files></files>
  <read_first>
    - docker-compose.yml (confirm PostgreSQL + Redis service definitions)
    - .planning/phases/02-the-big-merge/02-03-SUMMARY.md (Phase 2 validation results: 125/128 passed, 3 integration failures)
    - .planning/phases/03-validation-gate/03-CONTEXT.md (D-04: Docker must be running; D-05: zero tolerance)
  </read_first>
  <action>
    1. Start Docker infrastructure: run `docker compose up -d` from the repo root. Wait for PostgreSQL and Redis to be healthy (check with `docker compose ps` — both containers should show "running" or "healthy" status). Per D-04, Docker MUST be running before tests.

    2. Run build (per D-01, satisfies VALID-01):
       ```
       pnpm build
       ```
       Build MUST run before typecheck — workspace packages export types from dist/ which must exist first (per Phase 2 decision: "Build must precede typecheck").

    3. Run typecheck (per D-02, satisfies VALID-02):
       ```
       pnpm typecheck
       ```
       All 6 workspace packages must pass strict TypeScript checking.

    4. Run tests (per D-03, satisfies VALID-03):
       ```
       pnpm test
       ```
       All tests must pass including `health-check.integration.test.ts` (3 tests that require PostgreSQL). Per D-05, zero tolerance — no skipped tests, no partial passes.

    5. Record exact exit codes and output summaries for each command. If any command fails, capture the full error output for diagnosis.

    NOTE: Per D-06, Phase 2 already validated typecheck (6/6) + build (6/6) + 125/128 tests. This is a re-confirmation with Docker running. The 3 previously-failing integration tests (`health-check.integration.test.ts`) should now pass with PostgreSQL available.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && docker compose ps --format json | grep -q "running" && pnpm build && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows PostgreSQL and Redis containers in "running" state
    - `pnpm build` exits with code 0 — output includes "6 successful" or equivalent Turborepo summary showing all packages built
    - `pnpm typecheck` exits with code 0 — no TypeScript errors reported
    - `pnpm test` exits with code 0 — output shows 128 tests passed (or more if tests were added), 0 failures, 0 skipped
    - The 3 health-check integration tests that failed in Phase 2 now pass with PostgreSQL running
  </acceptance_criteria>
  <done>All three validation commands (build, typecheck, test) exit with code 0. All 128+ tests pass including PostgreSQL integration tests. Docker infrastructure is confirmed running.</done>
</task>

<task type="auto">
  <name>Task 2: Handle any discovered failures (conditional) or confirm clean gate</name>
  <files></files>
  <read_first>
    - Output from Task 1 (build, typecheck, test results)
    - .planning/phases/03-validation-gate/03-CONTEXT.md (D-05: zero tolerance)
  </read_first>
  <action>
    This task is conditional based on Task 1 results:

    **If all three commands passed (expected path):**
    - Confirm the validation gate is clean. No files to modify.
    - Report: "Phase 3 validation gate: PASSED. build=0, typecheck=0, test=0. All 128+ tests green."

    **If any command failed (unexpected, requires investigation):**
    - Diagnose the failure. Distinguish between:
      a) Merge-related regressions (should not exist — Phase 2 fixed all known issues)
      b) Environment issues (Docker connectivity, port conflicts, stale dist/ cache)
      c) Flaky tests (timing-dependent failures)
    - For environment issues: fix and re-run the failing command.
    - For code issues: fix with a targeted commit referencing the specific failure.
    - For flaky tests: re-run once. If it passes on retry, document the flakiness.
    - Re-run the failing validation command(s) until exit code 0. Per D-05, zero tolerance.

    After confirmation or fix, stop Docker if desired (optional — user may want it running for further work):
    - `docker compose down` (only if user prefers clean shutdown)
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm build && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - Final state: all three commands (build, typecheck, test) exit 0
    - If fixes were needed: committed with message referencing the specific failure
    - If no fixes needed: clean pass confirmed, no files modified
    - Zero skipped tests, zero partial passes (per D-05)
  </acceptance_criteria>
  <done>Validation gate definitively passes. VALID-01 (build), VALID-02 (typecheck), VALID-03 (test) are all satisfied. Phase 3 complete, Phase 4 unblocked.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the phase goal:
1. `pnpm build` — exits 0 (VALID-01)
2. `pnpm typecheck` — exits 0 (VALID-02)
3. `pnpm test` — exits 0, all tests pass including integration tests (VALID-03)
</verification>

<success_criteria>
- All three validation commands exit with code 0
- All 128+ unit and integration tests pass (zero failures, zero skipped)
- Docker infrastructure (PostgreSQL + Redis) confirmed running during test execution
- Phase 3 requirements VALID-01, VALID-02, VALID-03 satisfied
- Phase 4 (Push & Branch Cleanup) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-gate/03-01-SUMMARY.md`
</output>
