---
phase: 04-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/lib/cluster-watch-manager.ts
  - apps/api/src/lib/resource-watch-manager.ts
  - apps/api/src/lib/cluster-connection-state.ts
  - apps/api/src/routers/subscriptions.ts
  - apps/api/src/__tests__/subscriptions.test.ts
  - apps/api/src/routers/index.ts
  - apps/api/src/server.ts
  - apps/api/src/lib/k8s-watchers.ts
  - apps/api/src/lib/event-emitter.ts
  - apps/api/src/lib/k8s-units.ts
  - apps/api/src/jobs/deploy-smoke-test.ts
  - apps/web/next.config.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-03]

must_haves:
  truths:
    - "cluster-watch-manager.ts, resource-watch-manager.ts, cluster-connection-state.ts, and subscriptions.ts no longer exist in the codebase"
    - "No import statement in any live file references the deleted files"
    - "Dead emitter methods (emitPodEvent, emitMetrics, emitClusterStateChange, emitResourceChange) are removed from event-emitter.ts"
    - "pnpm build succeeds with ignoreBuildErrors removed from next.config.ts"
    - "pnpm typecheck passes with zero errors"
  artifacts:
    - path: "apps/api/src/routers/index.ts"
      provides: "Router registry without subscriptionsRouter"
      contains: "NOT subscriptionsRouter"
    - path: "apps/api/src/server.ts"
      provides: "Server startup without stopAllWatchers import"
      contains: "NOT stopAllWatchers"
    - path: "apps/api/src/lib/event-emitter.ts"
      provides: "Event emitter with only live methods"
      exports: ["voyagerEmitter"]
    - path: "apps/web/next.config.ts"
      provides: "Next.js config without ignoreBuildErrors"
      contains: "NOT ignoreBuildErrors"
  key_links:
    - from: "apps/api/src/lib/event-emitter.ts"
      to: "apps/api/src/lib/watch-manager.ts"
      via: "emitWatchEvent and emitWatchStatus still work"
      pattern: "emitWatchEvent|emitWatchStatus"
    - from: "apps/api/src/lib/k8s-watchers.ts"
      to: "apps/api/src/lib/event-emitter.ts"
      via: "emitDeploymentProgress and emitLogLine still work"
      pattern: "emitDeploymentProgress|emitLogLine"
---

<objective>
Remove ~1100 lines of dead code (4 legacy watcher files + 1 test file + dead methods in surviving files), clean orphaned dist artifacts, and enforce honest TypeScript build checking by removing ignoreBuildErrors.

Purpose: Eliminate code confusion from legacy watchers replaced by the unified WatchManager in Phases 1-3, and ensure future TypeScript errors are caught at build time.
Output: Clean codebase with no dead watcher code, honest build pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files and clean surviving file references</name>
  <read_first>
    - apps/api/src/routers/index.ts (find subscriptionsRouter import line 39 and registration line 74)
    - apps/api/src/server.ts (find stopAllWatchers import line 26 and call line 310)
    - apps/api/src/lib/k8s-watchers.ts (find streamLogs lines 124-174 and stopAllWatchers lines 178-181)
    - apps/api/src/lib/event-emitter.ts (full file — identify dead methods vs live methods)
    - apps/api/src/lib/k8s-units.ts (line 1 comment referencing cluster-watch-manager)
    - apps/api/src/jobs/deploy-smoke-test.ts (line 272 — orphaned 'deployment-event' listener)
  </read_first>
  <files>
    apps/api/src/lib/cluster-watch-manager.ts,
    apps/api/src/lib/resource-watch-manager.ts,
    apps/api/src/lib/cluster-connection-state.ts,
    apps/api/src/routers/subscriptions.ts,
    apps/api/src/__tests__/subscriptions.test.ts,
    apps/api/src/routers/index.ts,
    apps/api/src/server.ts,
    apps/api/src/lib/k8s-watchers.ts,
    apps/api/src/lib/event-emitter.ts,
    apps/api/src/lib/k8s-units.ts,
    apps/api/src/jobs/deploy-smoke-test.ts,
    apps/api/dist/lib/cluster-watch-manager.js,
    apps/api/dist/lib/cluster-watch-manager.js.map,
    apps/api/dist/lib/resource-watch-manager.js,
    apps/api/dist/lib/resource-watch-manager.js.map,
    apps/api/dist/lib/cluster-connection-state.js,
    apps/api/dist/lib/cluster-connection-state.js.map,
    apps/api/dist/routers/subscriptions.js,
    apps/api/dist/routers/subscriptions.js.map
  </files>
  <action>
    **Step 1 — Delete 5 dead source files:**
    - `apps/api/src/lib/cluster-watch-manager.ts` (300 lines)
    - `apps/api/src/lib/resource-watch-manager.ts` (309 lines)
    - `apps/api/src/lib/cluster-connection-state.ts` (63 lines)
    - `apps/api/src/routers/subscriptions.ts` (213 lines)
    - `apps/api/src/__tests__/subscriptions.test.ts` (238 lines)

    **Step 2 — Delete 8 compiled dist artifacts:**
    - `apps/api/dist/lib/cluster-watch-manager.js` + `.js.map`
    - `apps/api/dist/lib/resource-watch-manager.js` + `.js.map`
    - `apps/api/dist/lib/cluster-connection-state.js` + `.js.map`
    - `apps/api/dist/routers/subscriptions.js` + `.js.map`

    **Step 3 — Clean routers/index.ts:**
    - Remove import: `import { subscriptionsRouter } from './subscriptions.js'` (line 39)
    - Remove registration: `subscriptions: subscriptionsRouter,` (line 74)

    **Step 4 — Clean server.ts:**
    - Remove import: `import { stopAllWatchers } from './lib/k8s-watchers.js'` (line 26)
    - Remove call: `stopAllWatchers()` (line 310 inside SIGTERM/SIGINT handler)
    - Keep the surrounding `if (k8sEnabled) { ... stopDeploySmokeTest() }` block — just remove the single stopAllWatchers() line

    **Step 5 — Clean k8s-watchers.ts:**
    - Remove the deprecated `streamLogs()` function (lines 120-174) — only called by dead subscriptions router
    - Remove the deprecated `stopAllWatchers()` function (lines 176-181) — a no-op
    - Keep `watchDeploymentProgress()` (used by deploy-smoke-test) and `streamLogsFollow()` (used by log-stream route)

    **Step 6 — Clean event-emitter.ts:**
    - Remove 4 dead methods: `emitPodEvent()`, `emitMetrics()`, `emitClusterStateChange()`, `emitResourceChange()`
    - Remove their type imports from `@voyager/types`: `PodEvent`, `MetricsEvent`, `ClusterStateChangeEvent`, `ResourceChangeEvent`
    - Keep 6 live methods: `emitDeploymentProgress`, `emitAlert`, `emitLogLine`, `emitMetricsStream`, `emitWatchEvent`, `emitWatchStatus`
    - Keep their type imports: `AlertEvent`, `DeploymentProgressEvent`, `LogLineEvent`, `MetricsStreamEvent`, `WatchEvent`, `WatchStatusEvent`
    - Do NOT remove types from `@voyager/types` package itself — only remove unused imports from this file

    **Step 7 — Clean k8s-units.ts:**
    - Update line 1 comment from `/** Shared K8s unit parsers — used by metrics router and cluster-watch-manager */` to `/** Shared K8s unit parsers — used by metrics router, resource-mappers, and watch-db-writer */`

    **Step 8 — Clean deploy-smoke-test.ts:**
    - The `'deployment-event'` listener on line 272 is orphaned — it was only produced by the dead `cluster-watch-manager.ts`. The deploy-smoke-test's actual functionality works via `watchDeploymentProgress()` which emits `'deployment-progress'` (different event). Remove the orphaned `'deployment-event'` listener.
    - In `startDeploySmokeTest()`: remove `voyagerEmitter.on('deployment-event', ...)` listener (lines 272-274)
    - In `stopDeploySmokeTest()`: remove `voyagerEmitter.removeAllListeners('deployment-event')` (line 281)
    - Also remove the `DeploymentEvent` interface (line 9) and `handleDeploymentEvent()` function if they are ONLY used by the orphaned listener. Check if they serve any other purpose first.
    - Keep the `listening` flag pattern, the `pendingTimers` cleanup, and `seenGenerations.clear()`
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && ! test -f apps/api/src/lib/cluster-watch-manager.ts && ! test -f apps/api/src/lib/resource-watch-manager.ts && ! test -f apps/api/src/lib/cluster-connection-state.ts && ! test -f apps/api/src/routers/subscriptions.ts && ! test -f apps/api/src/__tests__/subscriptions.test.ts && echo "Dead files deleted" && ! grep -r "cluster-watch-manager" apps/api/src/ --include="*.ts" | grep -v "k8s-units" && echo "No imports to dead files" && pnpm typecheck && echo "Typecheck passed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "cluster-watch-manager" apps/api/src/ returns 0 results (except k8s-units.ts updated comment if grep picks it up — but it no longer references it)
    - grep -r "resource-watch-manager" apps/api/src/ returns 0 results
    - grep -r "cluster-connection-state" apps/api/src/ returns 0 results
    - grep -r "subscriptionsRouter" apps/api/src/ returns 0 results
    - grep -r "stopAllWatchers" apps/api/src/ returns 0 results
    - grep "emitPodEvent\|emitMetrics\b\|emitClusterStateChange\|emitResourceChange" apps/api/src/lib/event-emitter.ts returns 0 results
    - grep "emitWatchEvent\|emitWatchStatus\|emitDeploymentProgress\|emitAlert\|emitLogLine\|emitMetricsStream" apps/api/src/lib/event-emitter.ts returns 6 results (all live methods still present)
    - pnpm typecheck exits 0
  </acceptance_criteria>
  <done>All 5 dead source files and 8 dist artifacts deleted. References cleaned from routers/index.ts, server.ts, k8s-watchers.ts, event-emitter.ts, k8s-units.ts, and deploy-smoke-test.ts. pnpm typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ignoreBuildErrors and verify full build</name>
  <read_first>
    - apps/web/next.config.ts (lines 8-10 — the typescript.ignoreBuildErrors block)
  </read_first>
  <files>apps/web/next.config.ts</files>
  <action>
    Remove the entire `typescript` block from `next.config.ts`:
    ```
    typescript: {
      ignoreBuildErrors: true,
    },
    ```
    This is lines 8-10. After removal, the `nextConfig` object should go directly from `reactStrictMode: false,` to `output: 'standalone',`.

    Then run full build to verify no hidden TypeScript errors:
    ```bash
    pnpm build
    ```
    Research confirmed both `pnpm typecheck` and `pnpm build` pass with zero errors, so removal is safe.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && ! grep -q "ignoreBuildErrors" apps/web/next.config.ts && echo "ignoreBuildErrors removed" && pnpm build && echo "Build passed"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ignoreBuildErrors" apps/web/next.config.ts returns 0 results
    - pnpm build exits 0 with all pages compiling successfully
  </acceptance_criteria>
  <done>ignoreBuildErrors removed from next.config.ts. pnpm build succeeds with TypeScript checking enforced — future TS errors will fail the build instead of being silently ignored.</done>
</task>

</tasks>

<verification>
Full phase gate for CLEAN-01 and CLEAN-03:
1. `ls apps/api/src/lib/cluster-watch-manager.ts apps/api/src/lib/resource-watch-manager.ts apps/api/src/lib/cluster-connection-state.ts apps/api/src/routers/subscriptions.ts 2>&1` — all should report "No such file"
2. `pnpm typecheck` — exits 0
3. `pnpm build` — exits 0
4. `grep -r "ignoreBuildErrors" apps/web/` — returns 0 results
5. `grep -r "subscriptionsRouter\|stopAllWatchers\|cluster-watch-manager\|resource-watch-manager\|cluster-connection-state" apps/api/src/ --include="*.ts"` — returns 0 results
</verification>

<success_criteria>
- 5 dead source files + 8 dist artifacts removed (~1100 lines of dead code eliminated)
- All references to dead code cleaned from 6 surviving files
- ignoreBuildErrors removed from Next.js config
- pnpm typecheck and pnpm build both pass with zero errors
- No regressions — live functionality (watchDeploymentProgress, streamLogsFollow, all emitter methods used by WatchManager) still works
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup/04-01-SUMMARY.md`
</output>
