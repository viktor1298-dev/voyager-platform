---
phase: 04-push-branch-cleanup
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [.planning/branch-audit.txt]
autonomous: false
requirements: [CLEAN-03, CLEAN-04, CLEAN-05]
user_setup: []

must_haves:
  truths:
    - "worktree/ron and worktree/dima are verified as ancestors of main before deletion"
    - "feat/init-monorepo, worktree/ron, and worktree/dima remote branches are deleted"
    - "fix/v117-phase-d-r2 is documented in branch-audit.txt and deleted"
    - "Local stale branch claude/objective-shockley is deleted"
    - "git branch -r shows only origin/HEAD and origin/main"
  artifacts:
    - path: ".planning/branch-audit.txt"
      provides: "Documentation of discarded fix/v117-phase-d-r2 commit with supersession rationale"
      contains: "eaa87c6"
  key_links:
    - from: "origin/worktree/ron"
      to: "main"
      via: "git merge-base --is-ancestor"
      pattern: "merge-base --is-ancestor.*main"
    - from: "origin/worktree/dima"
      to: "main"
      via: "git merge-base --is-ancestor"
      pattern: "merge-base --is-ancestor.*main"
---

<objective>
Complete branch cleanup: verify and delete Batch 2 branches (feat/init-monorepo, worktree/ron, worktree/dima), document and delete Batch 3 (fix/v117-phase-d-r2), clean up local stale branches, and perform final verification.

Purpose: Reduce remote branches to only origin/main, completing the repository cleanup. Every branch deletion is gated on proof of redundancy (ancestry checks for Batch 2, supersession documentation for Batch 3).

Output: Clean repository with only origin/main remaining, branch-audit.txt documenting the fix/v117-phase-d-r2 disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-push-branch-cleanup/04-CONTEXT.md
@.planning/phases/04-push-branch-cleanup/04-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/branch-tips.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify ancestry and delete Batch 2 branches (CLEAN-05)</name>
  <files>N/A — git operations only</files>
  <read_first>.planning/research/ARCHITECTURE.md, .planning/branch-tips.txt</read_first>
  <action>
    Per D-04 and CLEAN-05, verify that worktree/ron and worktree/dima commits are fully contained in main (via the feat/init-monorepo merge), then delete all 3 Batch 2 branches.

    Step 1 — Ancestry verification (MUST pass before any deletion):
    ```bash
    git merge-base --is-ancestor origin/feat/init-monorepo main && echo "feat/init-monorepo: ANCESTOR OF MAIN" || echo "feat/init-monorepo: NOT ANCESTOR — ABORT"
    git merge-base --is-ancestor origin/worktree/ron main && echo "worktree/ron: ANCESTOR OF MAIN" || echo "worktree/ron: NOT ANCESTOR — ABORT"
    git merge-base --is-ancestor origin/worktree/dima main && echo "worktree/dima: ANCESTOR OF MAIN" || echo "worktree/dima: NOT ANCESTOR — ABORT"
    ```

    ALL THREE must print "ANCESTOR OF MAIN". If any prints "NOT ANCESTOR", STOP and report to user.

    Step 2 — Delete (only if all 3 pass):
    ```bash
    git push origin --delete feat/init-monorepo worktree/ron worktree/dima
    ```

    Step 3 — Prune tracking refs:
    ```bash
    git remote prune origin
    ```
  </action>
  <what-built>
    3 post-merge-redundant branches verified and deleted. feat/init-monorepo (the 54-commit integration branch), worktree/ron (50 commits, pure subset), and worktree/dima (45 commits, pure subset) are all proven ancestors of main via the Phase 2 merge. Branch tip SHAs preserved in .planning/branch-tips.txt.
  </what-built>
  <how-to-verify>
    1. Confirm all 3 ancestry checks printed "ANCESTOR OF MAIN"
    2. Confirm the deletion command completed successfully
    3. Run `git branch -r` — should show: origin/HEAD, origin/main, origin/fix/v117-phase-d-r2 (3 entries only)
  </how-to-verify>
  <verify>
    <automated>test "$(git branch -r | wc -l | tr -d ' ')" = "3" && echo "PASS: 3 remote refs remain (HEAD + main + fix/v117-phase-d-r2)" || echo "FAIL: unexpected remote branch count"</automated>
  </verify>
  <done>feat/init-monorepo, worktree/ron, and worktree/dima verified as ancestors of main and deleted from origin (CLEAN-05 complete)</done>
  <acceptance_criteria>
    - `git merge-base --is-ancestor origin/feat/init-monorepo main` exits 0
    - `git merge-base --is-ancestor origin/worktree/ron main` exits 0
    - `git merge-base --is-ancestor origin/worktree/dima main` exits 0
    - `git push origin --delete feat/init-monorepo worktree/ron worktree/dima` exits 0
    - `git branch -r` shows exactly 3 entries: origin/HEAD, origin/main, origin/fix/v117-phase-d-r2
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with Batch 3 and local cleanup, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Document and delete fix/v117-phase-d-r2, clean local branches, final verify (CLEAN-03, CLEAN-04)</name>
  <read_first>.planning/research/ARCHITECTURE.md, .planning/branch-tips.txt</read_first>
  <files>.planning/branch-audit.txt</files>
  <action>
    Per D-06 (CLEAN-03): Document fix/v117-phase-d-r2's unique commit eaa87c6 in .planning/branch-audit.txt BEFORE deletion. The commit adds encryption key to k8s and fixes connection-config schemas (v117-r2). It is superseded by fb5bb3c (v117-r3) which is already in main. No cherry-pick needed.

    Step 1 — Create .planning/branch-audit.txt with documentation:
    ```
    # Branch Audit — Phase 4 Cleanup
    # Recorded: [current date]

    ## Discarded Branches

    ### fix/v117-phase-d-r2
    - Branch tip: eaa87c6 (from .planning/branch-tips.txt: eaa87c69de31009340520dda23dc032aebcee14c)
    - Unique commit: eaa87c6 — "fix: apply encryption key to k8s, fix connection-config schemas, show lastConnectedAt in detail [v117-r2]"
    - Content: Encryption key handling for K8s cluster credentials, connection-config schema fixes, lastConnectedAt display in cluster detail
    - Superseded by: fb5bb3c — "fix: QA findings (v117-r3)" which is confirmed present in main
    - Decision: DISCARD — v117-r3 is a later revision that includes and supersedes the v117-r2 changes
    - Recovery: git push origin eaa87c69de31009340520dda23dc032aebcee14c:refs/heads/fix/v117-phase-d-r2
    ```

    Step 2 — Delete remote branch:
    ```bash
    git push origin --delete fix/v117-phase-d-r2
    ```

    Step 3 — Per D-07 (CLEAN-04), delete local stale branches:
    ```bash
    git branch -d claude/objective-shockley
    ```
    Use `-d` (safe delete) which only deletes if fully merged. If it fails, use `-D` after confirming the branch points to a commit already in main (SHA 31173c6 per branch-tips.txt, which is the old main HEAD before any Phase 1-3 work — fully contained in current main).

    Step 4 — Per D-09, final prune:
    ```bash
    git remote prune origin
    git fetch --prune
    ```

    Step 5 — Per D-08, final verification:
    ```bash
    git branch -r
    # Must show ONLY:
    #   origin/HEAD -> origin/main
    #   origin/main

    git branch -r | wc -l
    # Must be 2

    git branch
    # Must show only: * main
    ```
  </action>
  <verify>
    <automated>git branch -r | wc -l | xargs test 2 -eq && echo "PASS: only origin/HEAD + origin/main remain" || echo "FAIL: unexpected remote branches"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/branch-audit.txt exists and contains "eaa87c6", "superseded", "v117-r3", and recovery command
    - `git branch -r` shows exactly 2 entries: "origin/HEAD -> origin/main" and "origin/main"
    - `git branch` shows only "* main" (no stale local branches)
    - fix/v117-phase-d-r2 remote branch is gone
    - claude/objective-shockley local branch is gone
  </acceptance_criteria>
  <done>
    - fix/v117-phase-d-r2 documented in branch-audit.txt with full supersession rationale and recovery command, then deleted (CLEAN-03)
    - Local stale branch claude/objective-shockley deleted (CLEAN-04)
    - `git branch -r` shows only origin/HEAD and origin/main (D-08 verified)
    - Repository is fully clean: 1 remote branch (main), 1 local branch (main)
  </done>
</task>

</tasks>

<verification>
- `git merge-base --is-ancestor` confirmed for all Batch 2 branches before deletion (CLEAN-05)
- .planning/branch-audit.txt documents fix/v117-phase-d-r2 disposition (CLEAN-03)
- `git branch -r` shows only origin/HEAD + origin/main (D-08)
- `git branch` shows only main (CLEAN-04 — local cleanup complete)
- .planning/branch-tips.txt still intact for recovery insurance
</verification>

<success_criteria>
- Batch 2 branches (feat/init-monorepo, worktree/ron, worktree/dima) verified as ancestors of main and deleted (CLEAN-05)
- fix/v117-phase-d-r2 documented and deleted (CLEAN-03)
- Local branch claude/objective-shockley deleted (CLEAN-04)
- Remote: only origin/HEAD and origin/main remain
- Local: only main remains
- Phase 4 complete — repository fully cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/04-push-branch-cleanup/04-02-SUMMARY.md`
</output>
