---
phase: 04-synchronized-crosshair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/metrics/MetricsAreaChart.tsx
  - apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, VIZ-05]

must_haves:
  truths:
    - "Hovering any of the 4 panels shows a vertical crosshair line at the same timestamp on all panels simultaneously"
    - "Crosshair renders as a dashed vertical line across full chart height with a timestamp label"
    - "CPU and Memory panels show horizontal dashed reference lines at 65% (warning) and 85% (critical)"
    - "User can drag-to-select a time region on any panel to zoom into that range"
    - "User can click an expand icon on any panel to see it full-width"
  artifacts:
    - path: "apps/web/src/components/metrics/MetricsAreaChart.tsx"
      provides: "syncId prop, custom crosshair cursor, threshold ReferenceLine, Brush zoom, animationDuration=0"
      contains: "syncId"
    - path: "apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx"
      provides: "Expand toggle state, full-width panel rendering, brush-to-range callback"
      contains: "expandedPanel"
  key_links:
    - from: "MetricsAreaChart.tsx"
      to: "MetricsTimeSeriesPanel.tsx"
      via: "syncId='metrics-sync' prop on every AreaChart"
      pattern: "syncId.*metrics-sync"
    - from: "MetricsAreaChart.tsx"
      to: "MetricsTimeSeriesPanel.tsx"
      via: "onBrushChange callback wired to setRange"
      pattern: "onBrushChange"
    - from: "MetricsAreaChart.tsx"
      to: "globals.css"
      via: "CSS vars for threshold colors"
      pattern: "color-threshold-critical|color-threshold-warn"
---

<objective>
Add synchronized crosshair, threshold reference lines, brush zoom, and panel expand to the 4-panel metrics grid.

Purpose: Deliver the Grafana-signature cross-panel hover experience — hovering one chart highlights the same timestamp on all panels. Add operational context via threshold lines, zoom-into-anomalies via brush, and deep-dive via panel expand.

Output: Updated MetricsAreaChart.tsx with syncId, custom crosshair cursor, threshold lines, and Brush component. Updated MetricsTimeSeriesPanel.tsx with expand toggle and brush-to-range wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md

@apps/web/src/components/metrics/MetricsAreaChart.tsx
@apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx
@apps/web/src/stores/metrics-preferences.ts
@apps/web/src/components/metrics/TimeRangeSelector.tsx
@apps/web/src/hooks/useMetricsData.ts
@apps/web/src/lib/animation-constants.ts
@apps/web/src/components/charts/chart-theme.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/src/components/metrics/MetricsAreaChart.tsx:
```typescript
export interface MetricsDataPoint {
  timestamp: string
  bucketStart?: string | null
  bucketEnd?: string | null
  cpu: number | null
  memory: number | null
  pods: number | null
  networkBytesIn?: number | null
  networkBytesOut?: number | null
}

export type MetricKey = 'cpu' | 'memory' | 'pods' | 'networkIn' | 'networkOut'
export type MetricFamily = 'cpu' | 'memory' | 'network' | 'pods'

interface MetricsAreaChartProps {
  data: MetricsDataPoint[]
  range: MetricsRange
  activeMetrics?: MetricKey[]
  height?: number
}
```

From apps/web/src/components/metrics/TimeRangeSelector.tsx:
```typescript
export type MetricsRange = '5m' | '15m' | '30m' | '1h' | '3h' | '6h' | '12h' | '24h' | '2d' | '7d' | 'custom'
```

From apps/web/src/stores/metrics-preferences.ts:
```typescript
interface MetricsPreferencesState {
  range: MetricsRange
  setRange: (range: MetricsRange) => void
  // ...other fields
}
```

CSS variables already defined in globals.css:
```css
/* Dark theme */
--color-threshold-critical: #ef4444;
--color-threshold-warn: #f59e0b;
/* Light theme */
--color-threshold-critical: #dc2626;
--color-threshold-warn: #d97706;
```

Recharts 3.7.0 exports used: `AreaChart`, `Area`, `XAxis`, `YAxis`, `CartesianGrid`, `Tooltip`, `ResponsiveContainer`, `ReferenceLine`, `Brush`, `ReferenceArea`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: syncId crosshair + custom cursor + threshold reference lines in MetricsAreaChart</name>
  <files>apps/web/src/components/metrics/MetricsAreaChart.tsx</files>

  <read_first>
    - apps/web/src/components/metrics/MetricsAreaChart.tsx (full file — understand current AreaChart structure, imports, METRIC_CONFIG, CustomTooltip)
    - apps/web/src/app/globals.css lines 225-235 and 320-330 (threshold CSS vars for both themes)
    - apps/web/src/lib/animation-constants.ts (DURATION constants for transition timing)
    - .planning/research/PITFALLS.md Pitfall 2 (crosshair render cascade) and Pitfall 1 (key prop remount)
  </read_first>

  <action>
  Modify MetricsAreaChart.tsx to add synchronized crosshair, custom cursor, threshold lines, and brush zoom.

  **1. Add syncId prop to AreaChart (VIZ-01):**
  - Add new prop to MetricsAreaChartProps: `syncId?: string`
  - Pass `syncId={syncId}` to the `<AreaChart>` component. This is Recharts' built-in synchronization — all charts sharing the same syncId will show tooltips at the same X position on hover.

  **2. Custom crosshair cursor (VIZ-02):**
  - Create a `CustomCursor` component that renders a dashed vertical line across the full chart height with a timestamp label at the bottom.
  - The cursor receives `{ points, width, height, payload }` from Recharts Tooltip cursor prop.
  - Render: `<line>` SVG element from (x, 0) to (x, chartHeight), stroke="#666", strokeDasharray="3 3", strokeWidth=1.
  - Below the line at (x, chartHeight-16), render a small `<text>` element with the hovered timestamp formatted as HH:MM:SS, fill="var(--color-text-dim)", fontSize=10, fontFamily="monospace", textAnchor="middle".
  - Wire it: `<Tooltip cursor={<CustomCursor />} content={<CustomTooltip />} />`.
  - Memoize CustomCursor with React.memo to prevent unnecessary re-renders (Pitfall 2).

  **3. Threshold reference lines (VIZ-04):**
  - Add new prop to MetricsAreaChartProps: `showThresholds?: boolean`
  - When `showThresholds` is true AND the chart has a "percent" yAxis (cpu or memory panels):
    - Add `<ReferenceLine yAxisId="percent" y={85} stroke="var(--color-threshold-critical)" strokeDasharray="6 4" strokeWidth={1} label={false} />` for the 85% critical line.
    - Add `<ReferenceLine yAxisId="percent" y={65} stroke="var(--color-threshold-warn)" strokeDasharray="6 4" strokeWidth={1} label={false} />` for the 65% warning line.
  - Only render these when `primaryConfig?.yAxis === 'percent'` to avoid rendering on network/pods panels.

  **4. Brush zoom component (VIZ-03):**
  - Add new prop: `onBrushChange?: (startTimestamp: string, endTimestamp: string) => void`
  - Add `import { Brush } from 'recharts'` to the imports.
  - Render `<Brush dataKey="timestamp" height={24} stroke="var(--color-accent)" fill="var(--color-bg-card)" travellerWidth={8} onChange={(range) => { ... }} />` inside the AreaChart, after the Area elements.
  - The Brush `onChange` handler receives `{ startIndex, endIndex }`. Use these to look up `chartData[startIndex].timestamp` and `chartData[endIndex].timestamp`, then call `onBrushChange(startTs, endTs)`.
  - Style the brush: height=24, compact appearance, only show when data.length > 5 to avoid useless brush on tiny datasets.

  **5. Fix key prop remount bug (Pitfall 1 from PITFALLS.md):**
  - Change the AreaChart key from `key={\`${range}-${activeMetrics.join('-')}-${data.length}\`}` to `key={\`${range}-${activeMetrics.join('-')}\`}`. Remove `data.length` — it causes full SVG teardown on every data update.

  **6. Add animationDuration={0} (Pitfall 1):**
  - On each `<Area>` element, add `animationDuration={0}` alongside the existing `isAnimationActive={false}`. This eliminates the known 1500ms dot render delay.

  **7. Add debounce to ResponsiveContainer (Pitfall 7):**
  - Change `<ResponsiveContainer width="100%" height={height}>` to `<ResponsiveContainer width="100%" height={height} debounce={100}>`. This prevents resize thrashing during sidebar collapse.

  Do NOT change the CustomTooltip content or the CurrentValueBadge component — those stay as-is.
  Do NOT attempt a custom Zustand/ref crosshair state — start with Recharts syncId (simpler). If perf issues arise, that is Phase 7 optimization work.
  </action>

  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -q "syncId" apps/web/src/components/metrics/MetricsAreaChart.tsx (syncId prop exists)
    - grep -q "CustomCursor" apps/web/src/components/metrics/MetricsAreaChart.tsx (custom cursor component exists)
    - grep -q "strokeDasharray" apps/web/src/components/metrics/MetricsAreaChart.tsx (dashed line rendering)
    - grep -q "color-threshold-critical" apps/web/src/components/metrics/MetricsAreaChart.tsx (critical threshold line)
    - grep -q "color-threshold-warn" apps/web/src/components/metrics/MetricsAreaChart.tsx (warning threshold line)
    - grep -q "Brush" apps/web/src/components/metrics/MetricsAreaChart.tsx (brush zoom component)
    - grep -q "onBrushChange" apps/web/src/components/metrics/MetricsAreaChart.tsx (brush callback prop)
    - grep -q "animationDuration={0}" apps/web/src/components/metrics/MetricsAreaChart.tsx (dot delay fix)
    - grep -q "debounce={100}" apps/web/src/components/metrics/MetricsAreaChart.tsx (resize debounce)
    - NOT grep -q "data.length" apps/web/src/components/metrics/MetricsAreaChart.tsx (key prop bug removed)
    - pnpm typecheck exits 0
  </acceptance_criteria>

  <done>
    MetricsAreaChart accepts syncId, showThresholds, and onBrushChange props. CustomCursor renders a dashed vertical line with timestamp label. Threshold reference lines render at 65% and 85% on percent-axis charts. Brush component enables drag-to-select. Key prop bug fixed. ResponsiveContainer debounced. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire syncId + brush zoom + panel fullscreen expand in MetricsTimeSeriesPanel</name>
  <files>apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx</files>

  <read_first>
    - apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (full file — understand PANEL_METRICS, grid rendering, how MetricsAreaChart is called)
    - apps/web/src/stores/metrics-preferences.ts (setRange, setCustomRange functions)
    - apps/web/src/components/metrics/MetricsAreaChart.tsx (after Task 1 modifications — new props: syncId, showThresholds, onBrushChange)
  </read_first>

  <action>
  Modify MetricsTimeSeriesPanel.tsx to wire the new MetricsAreaChart props and add panel expand functionality.

  **1. Pass syncId="metrics-sync" to all MetricsAreaChart instances (VIZ-01):**
  - In the PANEL_METRICS.map() render block, add `syncId="metrics-sync"` to every `<MetricsAreaChart>` call. All 4 panels share this ID so Recharts synchronizes their tooltips.

  **2. Pass showThresholds to CPU and Memory panels (VIZ-04):**
  - In the PANEL_METRICS.map() render block, add `showThresholds={panel.id === 'cpu' || panel.id === 'memory'}` to each `<MetricsAreaChart>`. Only CPU and Memory use percent axes; Network and Pods do not get threshold lines.

  **3. Wire brush zoom to time range (VIZ-03):**
  - Create a `handleBrushZoom` callback: `(startTs: string, endTs: string) => { setCustomRange(startTs, endTs) }`.
  - `setCustomRange` comes from the existing `useMetricsPreferences()` hook (already destructured in the component). It sets range='custom' and stores the from/to timestamps.
  - Pass `onBrushChange={handleBrushZoom}` to every `<MetricsAreaChart>`.
  - Wrap the callback in `useCallback` with `[setCustomRange]` deps.

  **4. Panel fullscreen expand (VIZ-05):**
  - Add state: `const [expandedPanel, setExpandedPanel] = useState<string | null>(null)`.
  - Import `Maximize2` and `Minimize2` icons from `lucide-react`.
  - In each panel's header (`<div className="mb-3 flex items-start justify-between gap-3">`), add an expand/collapse button to the right side:
    ```tsx
    <button
      type="button"
      onClick={() => setExpandedPanel(expandedPanel === panel.id ? null : panel.id)}
      className="rounded-md p-1 text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-text-primary)] hover:bg-white/5"
      aria-label={expandedPanel === panel.id ? 'Collapse panel' : 'Expand panel'}
    >
      {expandedPanel === panel.id ? <Minimize2 className="h-4 w-4" /> : <Maximize2 className="h-4 w-4" />}
    </button>
    ```
  - Modify the grid container class: when `expandedPanel` is set, hide all panels except the expanded one and make it full-width. Use conditional className:
    - Grid wrapper: `className={cn("grid gap-4", expandedPanel ? "md:grid-cols-1" : "md:grid-cols-2")}`
    - Each panel section: if `expandedPanel` is set and `panel.id !== expandedPanel`, add `className="hidden"` (or skip rendering via a filter).
    - The simpler approach: filter PANEL_METRICS before mapping — `const visiblePanels = expandedPanel ? PANEL_METRICS.filter(p => p.id === expandedPanel) : PANEL_METRICS`. Then always use `md:grid-cols-1` when expanded, `md:grid-cols-2` when not.
  - When expanded, increase chart height: `const chartHeight = expandedPanel ? 400 : (compact ? 200 : 240)`.

  **5. Update the section key to NOT include range (prevents remount on range change):**
  - Change `key={\`${panel.id}-${range}-${activeMetrics.join('-')}\`}` to `key={panel.id}`. The range is passed as a prop to MetricsAreaChart; changing the key forces a full section remount which is wasteful.

  Do NOT modify the loading/error/empty state blocks — they stay as-is.
  Do NOT change the series toggle buttons or the header layout — only add the expand button.
  Do NOT change auto-refresh or TimeRangeSelector wiring.
  </action>

  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck 2>&1 | tail -5 && pnpm build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -q 'syncId="metrics-sync"' apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (syncId wired)
    - grep -q "showThresholds" apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (threshold prop passed)
    - grep -q "onBrushChange" apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (brush callback wired)
    - grep -q "expandedPanel" apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (expand state exists)
    - grep -q "Maximize2" apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (expand icon imported)
    - grep -q "Minimize2" apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (collapse icon imported)
    - grep -q "handleBrushZoom" apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx (brush handler)
    - pnpm typecheck exits 0
    - pnpm build exits 0
  </acceptance_criteria>

  <done>
    All 4 metric panels share syncId="metrics-sync" for synchronized crosshair. CPU and Memory panels show 65%/85% threshold lines. Brush drag-to-select updates the time range to a custom window. Each panel has an expand/collapse icon that toggles between 2-column grid and full-width single panel. Build and typecheck pass clean.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` — zero errors
2. `pnpm build` — all pages compile successfully
3. Grep verification of all acceptance criteria (automated via acceptance_criteria blocks)
4. Visual verification: start dev servers (`pnpm dev`), navigate to any cluster's metrics tab, confirm:
   - Hovering one panel shows crosshair on all 4 panels
   - Crosshair is a dashed vertical line with timestamp label
   - CPU and Memory panels show 65% yellow dashed and 85% red dashed horizontal lines
   - Dragging on any panel's brush selects a time range
   - Expand icon in top-right of each panel toggles full-width view
</verification>

<success_criteria>
- All 5 VIZ requirements (VIZ-01 through VIZ-05) are implemented
- TypeScript compiles clean, production build succeeds
- No new dependencies added (all features use existing Recharts 3.7.0 components + lucide-react icons)
- Key prop remount bug fixed (data.length removed from key)
- ResponsiveContainer debounced at 100ms
</success_criteria>

<output>
After completion, create `.planning/phases/04-synchronized-crosshair/04-01-SUMMARY.md`
</output>
