---
phase: 08-resource-explorer-ux-overhaul
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/src/app/clusters/[id]/cronjobs/page.tsx
  - apps/web/src/app/clusters/[id]/hpa/page.tsx
  - apps/web/src/app/clusters/[id]/configmaps/page.tsx
  - apps/web/src/app/clusters/[id]/secrets/page.tsx
  - apps/web/src/app/clusters/[id]/pvcs/page.tsx
  - apps/web/src/app/clusters/[id]/namespaces/page.tsx
  - apps/web/src/app/clusters/[id]/events/page.tsx
  - apps/web/src/app/clusters/[id]/pods/page.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-15

must_haves:
  truths:
    - "CronJobs, HPA, ConfigMaps, Secrets, PVCs tabs show namespace-grouped cards with search/filter and expand-all"
    - "Namespaces tab shows flat card list (no namespace grouping) with search/filter and expand-all"
    - "Events tab shows cards grouped by involvedObject.namespace with search/filter and expand-all"
    - "Pods tab now has Expand All / Collapse All toggle in its search bar"
    - "Karpenter/autoscaling tab is completely unchanged"
  artifacts:
    - path: "apps/web/src/app/clusters/[id]/cronjobs/page.tsx"
      provides: "CronJobs with namespace-grouped cards"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/hpa/page.tsx"
      provides: "HPA with namespace-grouped cards"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/configmaps/page.tsx"
      provides: "ConfigMaps with namespace-grouped cards"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/secrets/page.tsx"
      provides: "Secrets with namespace-grouped cards"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/pvcs/page.tsx"
      provides: "PVCs with namespace-grouped cards"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/namespaces/page.tsx"
      provides: "Namespaces with flat card list"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/events/page.tsx"
      provides: "Events grouped by namespace"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/pods/page.tsx"
      provides: "Pods tab with expand-all toggle added"
      contains: "SearchFilterBar"
  key_links:
    - from: "namespaces/page.tsx"
      to: "ResourcePageScaffold"
      via: "flatList={true} prop"
      pattern: "flatList.*true"
    - from: "events/page.tsx"
      to: "ResourcePageScaffold"
      via: "getNamespace extracts involvedObject.namespace"
      pattern: "getNamespace"
    - from: "pods/page.tsx"
      to: "SearchFilterBar"
      via: "import and render in place of inline search"
      pattern: "SearchFilterBar"
---

<objective>
Rewrite 5 remaining resource tabs (CronJobs, HPA, ConfigMaps, Secrets, PVCs) to use ResourcePageScaffold, convert the 2 special-case pages (Namespaces as flat list, Events grouped by involvedObject.namespace), and enhance the Pods page with expand-all toggle. This completes the UX unification across all resource tabs.

Purpose: Finish the tab redesign (per D-01, D-02, D-03) so every resource tab in the cluster detail view has a consistent look and feel matching the Pods gold standard.
Output: 8 rewritten/enhanced page files. All resource tabs now unified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-resource-explorer-ux-overhaul/08-CONTEXT.md
@.planning/phases/08-resource-explorer-ux-overhaul/08-01-SUMMARY.md

<interfaces>
From apps/web/src/components/resource/ResourcePageScaffold.tsx:
```typescript
interface ResourcePageScaffoldProps<T> {
  title: string
  icon?: ReactNode
  data: T[] | undefined
  isLoading: boolean
  hasCredentials: boolean
  getNamespace: (item: T) => string
  getKey: (item: T) => string
  filterFn: (item: T, query: string) => boolean
  renderSummary: (item: T) => ReactNode
  renderDetail: (item: T) => ReactNode
  searchPlaceholder: string
  emptyMessage: string
  emptyDescription?: string
  emptyIcon?: ReactNode
  flatList?: boolean
}
```

From apps/web/src/components/resource/SearchFilterBar.tsx:
```typescript
interface SearchFilterBarProps {
  searchQuery: string
  onSearchChange: (query: string) => void
  placeholder: string
  filteredCount: number
  totalCount: number
  expandAll: boolean
  onExpandAllChange: (expandAll: boolean) => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CronJobs, HPA, ConfigMaps, Secrets, PVCs pages</name>
  <files>
    apps/web/src/app/clusters/[id]/cronjobs/page.tsx
    apps/web/src/app/clusters/[id]/hpa/page.tsx
    apps/web/src/app/clusters/[id]/configmaps/page.tsx
    apps/web/src/app/clusters/[id]/secrets/page.tsx
    apps/web/src/app/clusters/[id]/pvcs/page.tsx
  </files>
  <read_first>
    apps/web/src/app/clusters/[id]/cronjobs/page.tsx
    apps/web/src/app/clusters/[id]/hpa/page.tsx
    apps/web/src/app/clusters/[id]/configmaps/page.tsx
    apps/web/src/app/clusters/[id]/secrets/page.tsx
    apps/web/src/app/clusters/[id]/pvcs/page.tsx
    apps/web/src/components/resource/ResourcePageScaffold.tsx
    apps/web/src/app/clusters/[id]/services/page.tsx
  </read_first>
  <action>
**Same pattern as Plan 02 — apply ResourcePageScaffold to 5 more pages.**

For each page: keep existing data interface, helper functions, and expanded detail component. Replace page component + table rendering with ResourcePageScaffold. Create `*Summary` component.

**cronjobs/page.tsx:**
- Keep: `CronJobData` interface, `CronJobExpandedDetail`
- Summary: name (mono bold), schedule (mono, dim), suspend badge (if suspended, amber "Suspended"), last schedule time, age
- Filter: name, namespace, schedule
- searchPlaceholder: `"Filter cronjobs by name, namespace, or schedule..."`
- emptyMessage: `"No CronJobs found in this cluster"`

**hpa/page.tsx:**
- Keep: `HPAData` interface, `HPAExpandedDetail`
- Summary: name (mono bold), target reference (accent color), min/max replicas badge, current replicas, age
- Filter: name, namespace, target
- searchPlaceholder: `"Filter autoscalers by name, namespace, or target..."`
- emptyMessage: `"No Horizontal Pod Autoscalers found in this cluster"`

**configmaps/page.tsx:**
- Keep: `ConfigMapData` interface, `ConfigMapExpandedDetail`
- Summary: name (mono bold), data keys count badge (`{n} keys`), age
- Filter: name, namespace
- searchPlaceholder: `"Filter configmaps by name or namespace..."`
- emptyMessage: `"No ConfigMaps found in this cluster"`

**secrets/page.tsx:**
- Keep: `SecretData` interface, `SecretExpandedDetail`
- Summary: name (mono bold), type badge (accent pill), data keys count, age
- Filter: name, namespace, type
- searchPlaceholder: `"Filter secrets by name, namespace, or type..."`
- emptyMessage: `"No Secrets found in this cluster"`

**pvcs/page.tsx:**
- Keep: `PVCData` interface, `PVCExpandedDetail`
- Summary: name (mono bold), status badge (Bound in green, Pending in amber, Lost in red), capacity, storage class (dim), age
- Filter: name, namespace, status, storageClass
- searchPlaceholder: `"Filter PVCs by name, namespace, or status..."`
- emptyMessage: `"No Persistent Volume Claims found in this cluster"`

For all 5: remove `ExpandableTableRow` imports, remove `<table>` markup, import `ResourcePageScaffold` from `@/components/resource`.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - cronjobs/page.tsx contains `ResourcePageScaffold`
    - cronjobs/page.tsx does NOT contain `ExpandableTableRow`
    - hpa/page.tsx contains `ResourcePageScaffold`
    - hpa/page.tsx does NOT contain `ExpandableTableRow`
    - configmaps/page.tsx contains `ResourcePageScaffold`
    - configmaps/page.tsx does NOT contain `ExpandableTableRow`
    - secrets/page.tsx contains `ResourcePageScaffold`
    - secrets/page.tsx does NOT contain `ExpandableTableRow`
    - pvcs/page.tsx contains `ResourcePageScaffold`
    - pvcs/page.tsx does NOT contain `ExpandableTableRow`
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>CronJobs, HPA, ConfigMaps, Secrets, PVCs all use ResourcePageScaffold with namespace-grouped cards.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Namespaces (flat), Events (namespace-grouped), and add expand-all to Pods</name>
  <files>
    apps/web/src/app/clusters/[id]/namespaces/page.tsx
    apps/web/src/app/clusters/[id]/events/page.tsx
    apps/web/src/app/clusters/[id]/pods/page.tsx
  </files>
  <read_first>
    apps/web/src/app/clusters/[id]/namespaces/page.tsx
    apps/web/src/app/clusters/[id]/events/page.tsx
    apps/web/src/app/clusters/[id]/pods/page.tsx
    apps/web/src/components/resource/ResourcePageScaffold.tsx
    apps/web/src/components/resource/SearchFilterBar.tsx
  </read_first>
  <action>
**1. namespaces/page.tsx — Flat card list (per D-02):**

Rewrite to use ResourcePageScaffold with `flatList={true}`:
- Keep: `NamespaceData` interface, `NamespaceExpanded` detail component
- Since namespaces ARE the namespace dimension, there's no namespace grouping. Use `flatList={true}`.
- `getNamespace`: return `'all'` (required by type but unused with flatList)
- `getKey`: `(ns) => ns.name`
- Summary: name (mono bold), status badge (Active in green, Terminating in red), resource quota summary if present, age
- Filter: name, status
- searchPlaceholder: `"Filter namespaces by name or status..."`
- emptyMessage: `"No Namespaces found in this cluster"`
- Remove `ExpandableTableRow` import and table markup

**2. events/page.tsx — Group by involvedObject.namespace (per D-02):**

Rewrite to use ResourcePageScaffold:
- Keep: `EventData` interface, `EventExpanded` detail component
- `getNamespace`: `(event) => event.namespace || 'cluster'` — cluster-wide events go under "cluster" group
- `getKey`: `(event) => \`${event.namespace}/${event.involvedObject}/${event.reason}/${event.lastTimestamp}\``
- Summary: type badge (Warning in amber, Normal in green), reason (bold), truncated message, involved object, count (if >1), age
- Filter: reason, message, namespace, involvedObject, type
- searchPlaceholder: `"Filter events by reason, message, or object..."`
- emptyMessage: `"No Events found in this cluster"`
- Keep the 10s `refetchInterval` (events are fast-moving)
- Remove `ExpandableTableRow` import and table markup

**3. pods/page.tsx — Add expand-all toggle (per D-03):**

DO NOT rewrite the Pods page to use ResourcePageScaffold. It has custom behavior (live data, offline banner, delete dialog) that doesn't fit the scaffold. Instead, make targeted enhancements:

a. Import `SearchFilterBar` from `@/components/resource`
b. Replace the inline search bar (lines 457-472 of current file) with `<SearchFilterBar>`
c. Add `const [expandAll, setExpandAll] = useState<boolean | null>(null)` state
d. Pass `expandAll` and `onExpandAllChange` to SearchFilterBar
e. In `NamespacePodGroup`, pass `expandAll` down to each `<ExpandableCard>`:
   - `expanded={expandAll ?? undefined}` — when null, cards are uncontrolled; when true/false, cards are controlled
   - `onExpandedChange={() => setExpandAll(null)}` — clicking individual card resets to uncontrolled mode
f. Update `NamespacePodGroup` props to accept `expandAll: boolean | null`

The rest of the Pods page stays exactly as-is: live data handling, offline banner, delete dialog, PodDetail, PodSummary, all preserved.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - namespaces/page.tsx contains `ResourcePageScaffold`
    - namespaces/page.tsx contains `flatList`
    - namespaces/page.tsx does NOT contain `ExpandableTableRow`
    - events/page.tsx contains `ResourcePageScaffold`
    - events/page.tsx contains `getNamespace` with `'cluster'` fallback
    - events/page.tsx does NOT contain `ExpandableTableRow`
    - pods/page.tsx contains `import.*SearchFilterBar.*from.*@/components/resource`
    - pods/page.tsx contains `expandAll` state variable
    - pods/page.tsx does NOT contain `ResourcePageScaffold` (it's enhanced, not rewritten)
    - pods/page.tsx still contains `DeletePodDialog` (preserved)
    - pods/page.tsx still contains `NamespacePodGroup` (preserved with expand-all prop added)
    - autoscaling/page.tsx is NOT in files_modified (per UX-15, Karpenter unchanged)
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Namespaces shows flat cards, Events groups by involvedObject.namespace, Pods has expand-all toggle. All 3 build clean. Karpenter untouched.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` exits 0
- `pnpm build` exits 0
- All 7 rewritten pages import from `@/components/resource`
- No page in this plan imports `ExpandableTableRow` (except Pods which doesn't use it anyway)
- `apps/web/src/app/clusters/[id]/autoscaling/page.tsx` has not been modified (verify via `git diff`)
</verification>

<success_criteria>
- All 12 resource tabs (10 namespaced + Namespaces flat + Events namespace-grouped) now use ExpandableCard layout
- Pods page has expand-all toggle integrated into its existing search bar
- Karpenter/autoscaling tab is completely untouched
- Build and typecheck pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-resource-explorer-ux-overhaul/08-03-SUMMARY.md`
</output>
