---
phase: 08-resource-explorer-ux-overhaul
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/src/app/clusters/[id]/daemonsets/page.tsx
  - apps/web/src/app/clusters/[id]/jobs/page.tsx
  - apps/web/src/app/clusters/[id]/cronjobs/page.tsx
  - apps/web/src/app/clusters/[id]/hpa/page.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "DaemonSets page shows namespace-grouped cards with search/filter and expand-all matching Pods design"
    - "Jobs page shows namespace-grouped cards with search/filter and expand-all matching Pods design"
    - "CronJobs page shows namespace-grouped cards with search/filter and expand-all matching Pods design"
    - "HPA page shows namespace-grouped cards with search/filter and expand-all matching Pods design"
  artifacts:
    - path: "apps/web/src/app/clusters/[id]/daemonsets/page.tsx"
      provides: "DaemonSets card layout"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/jobs/page.tsx"
      provides: "Jobs card layout"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/cronjobs/page.tsx"
      provides: "CronJobs card layout"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/hpa/page.tsx"
      provides: "HPA card layout"
      contains: "ResourcePageScaffold"
  key_links:
    - from: "daemonsets/page.tsx"
      to: "ResourcePageScaffold"
      via: "import from @/components/resource"
      pattern: "import.*ResourcePageScaffold.*from.*@/components/resource"
---

<objective>
Redesign DaemonSets, Jobs, CronJobs, and HPA pages from ExpandableTableRow table layout to Pods-style namespace-grouped card layout using ResourcePageScaffold from Plan 01.

Purpose: Second batch of tab redesigns (per D-01). These workload and autoscaling resources complete the middle tier of resource types.
Output: 4 rewritten page files using ResourcePageScaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-resource-explorer-ux-overhaul/08-CONTEXT.md
@.planning/phases/08-resource-explorer-ux-overhaul/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From apps/web/src/components/resource/ResourcePageScaffold.tsx:
```typescript
interface ResourcePageScaffoldProps<T> {
  title: string
  icon: ReactNode
  queryResult: { data: T[] | undefined; isLoading: boolean; error: unknown }
  getNamespace: (item: T) => string
  getKey: (item: T) => string
  filterFn: (item: T, query: string) => boolean
  renderSummary: (item: T) => ReactNode
  renderDetail: (item: T) => ReactNode
  searchPlaceholder?: string
  emptyMessage?: string
  emptyDescription?: string
  flatList?: boolean
}
```

From tRPC router keys:
- trpc.daemonSets.list.useQuery({ clusterId })
- trpc.jobs.list.useQuery({ clusterId })
- trpc.cronJobs.list.useQuery({ clusterId })
- trpc.hpa.list.useQuery({ clusterId })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DaemonSets and Jobs pages</name>
  <files>
    apps/web/src/app/clusters/[id]/daemonsets/page.tsx
    apps/web/src/app/clusters/[id]/jobs/page.tsx
  </files>
  <read_first>
    - apps/web/src/app/clusters/[id]/daemonsets/page.tsx
    - apps/web/src/app/clusters/[id]/jobs/page.tsx
    - apps/web/src/components/resource/ResourcePageScaffold.tsx
    - docs/DESIGN.md
  </read_first>
  <action>
**DaemonSets page rewrite** (`daemonsets/page.tsx`):

Keep existing interface types. Replace render with ResourcePageScaffold.
- `DaemonSetSummary`: name (bold), desired/ready/available count badges, status pill (Ready if desired==ready, else Updating), image, age
- `DaemonSetExpandedDetail`: Status (desired/current/ready/available), Node Selector, Tolerations, Conditions, Labels tabs via DetailTabs
- Icon: `Layers` from lucide-react
- filterFn: search by name, namespace, status
- searchPlaceholder: "Search daemonsets..."

**Jobs page rewrite** (`jobs/page.tsx`):

Keep existing interface types. Replace render with ResourcePageScaffold.
- `JobSummary`: name (bold), completions badge (`{succeeded}/{completions}`), status pill (Complete/Running/Failed), duration, age
- `JobExpandedDetail`: Status (active/succeeded/failed/startTime/completionTime), Spec (parallelism, backoffLimit, ttlSecondsAfterFinished), Conditions, Labels tabs via DetailTabs
- Icon: `Play` from lucide-react
- filterFn: search by name, namespace, status
- searchPlaceholder: "Search jobs..."

Both pages: NO ExpandableTableRow. ResourcePageScaffold only. CSS vars for colors. B-style animations from DESIGN.md.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/daemonsets/page.tsx
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/jobs/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/daemonsets/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/jobs/page.tsx
    - pnpm typecheck passes with 0 errors
  </acceptance_criteria>
  <done>DaemonSets and Jobs pages rewritten with namespace-grouped card layout using ResourcePageScaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign CronJobs and HPA pages</name>
  <files>
    apps/web/src/app/clusters/[id]/cronjobs/page.tsx
    apps/web/src/app/clusters/[id]/hpa/page.tsx
  </files>
  <read_first>
    - apps/web/src/app/clusters/[id]/cronjobs/page.tsx
    - apps/web/src/app/clusters/[id]/hpa/page.tsx
    - apps/web/src/components/resource/ResourcePageScaffold.tsx
    - docs/DESIGN.md
  </read_first>
  <action>
**CronJobs page rewrite** (`cronjobs/page.tsx`):

Keep existing interface types. Replace render with ResourcePageScaffold.
- `CronJobSummary`: name (bold), schedule (monospace), last schedule time (relative), suspend badge (if suspended), active count, age
- `CronJobExpandedDetail`: Schedule info, Concurrency Policy, Job Template, History (successfulJobsHistoryLimit, failedJobsHistoryLimit), Labels tabs via DetailTabs
- Icon: `Clock` from lucide-react
- filterFn: search by name, namespace, schedule
- searchPlaceholder: "Search cronjobs..."

**HPA page rewrite** (`hpa/page.tsx`):

Keep existing interface types. Replace render with ResourcePageScaffold.
- `HPASummary`: name (bold), target reference (kind/name), min/max replicas, current replicas, current CPU/memory metrics badges
- `HPAExpandedDetail`: Metrics (current vs target for each metric), Scale Target, Min/Max, Behavior (scaleUp/scaleDown policies), Conditions, Labels tabs via DetailTabs
- Icon: `TrendingUp` from lucide-react
- filterFn: search by name, namespace, target name
- searchPlaceholder: "Search HPAs..."

Both pages: NO ExpandableTableRow. ResourcePageScaffold only. CSS vars for colors.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/cronjobs/page.tsx
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/hpa/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/cronjobs/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/hpa/page.tsx
    - pnpm typecheck passes with 0 errors
    - pnpm build passes with 0 errors
  </acceptance_criteria>
  <done>CronJobs and HPA pages rewritten with namespace-grouped card layout. All 4 pages in this plan use ResourcePageScaffold. pnpm build passes.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm build` both pass
- All 4 pages import ResourcePageScaffold from `@/components/resource`
- None of the 4 pages import ExpandableTableRow
- Each page has resource-specific Summary and ExpandedDetail components
</verification>

<success_criteria>
DaemonSets, Jobs, CronJobs, and HPA pages all use Pods-style namespace-grouped card layout via ResourcePageScaffold. pnpm build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-resource-explorer-ux-overhaul/08-04-SUMMARY.md`
</output>
