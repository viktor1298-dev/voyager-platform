---
phase: 08-resource-explorer-ux-overhaul
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/src/app/clusters/[id]/configmaps/page.tsx
  - apps/web/src/app/clusters/[id]/secrets/page.tsx
  - apps/web/src/app/clusters/[id]/pvcs/page.tsx
  - apps/web/src/app/clusters/[id]/namespaces/page.tsx
  - apps/web/src/app/clusters/[id]/events/page.tsx
  - apps/web/src/app/clusters/[id]/pods/page.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "ConfigMaps page shows namespace-grouped cards with search/filter and expand-all"
    - "Secrets page shows namespace-grouped cards with search/filter and expand-all"
    - "PVCs page shows namespace-grouped cards with search/filter and expand-all"
    - "Namespaces page shows flat card list (no namespace grouping) with search/filter and expand-all"
    - "Events page groups by involvedObject.namespace with search/filter and expand-all"
    - "Pods page has Expand All / Collapse All toggle button added"
  artifacts:
    - path: "apps/web/src/app/clusters/[id]/configmaps/page.tsx"
      provides: "ConfigMaps card layout"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/secrets/page.tsx"
      provides: "Secrets card layout"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/pvcs/page.tsx"
      provides: "PVCs card layout"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/namespaces/page.tsx"
      provides: "Namespaces flat card layout"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/events/page.tsx"
      provides: "Events card layout grouped by namespace"
      contains: "ResourcePageScaffold"
    - path: "apps/web/src/app/clusters/[id]/pods/page.tsx"
      provides: "Pods page with expand-all toggle"
      contains: "expandAll"
  key_links:
    - from: "namespaces/page.tsx"
      to: "ResourcePageScaffold"
      via: "flatList={true}"
      pattern: "flatList"
    - from: "events/page.tsx"
      to: "ResourcePageScaffold"
      via: "getNamespace using involvedObject.namespace"
      pattern: "involvedObject"
---

<objective>
Redesign ConfigMaps, Secrets, PVCs pages to namespace-grouped card layout. Handle special cases: Namespaces page (flat card list per D-02), Events page (group by involvedObject.namespace per D-02). Add Expand All / Collapse All to existing Pods page (per D-03).

Purpose: Complete the remaining resource tab redesigns including the two special-case pages and the Pods page enhancement.
Output: 5 rewritten + 1 enhanced page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-resource-explorer-ux-overhaul/08-CONTEXT.md
@.planning/phases/08-resource-explorer-ux-overhaul/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From apps/web/src/components/resource/ResourcePageScaffold.tsx:
```typescript
interface ResourcePageScaffoldProps<T> {
  // ... standard props
  flatList?: boolean  // Skip namespace grouping — for Namespaces page
}
```

From tRPC router keys:
- trpc.configMaps.list.useQuery({ clusterId })
- trpc.secrets.list.useQuery({ clusterId })
- trpc.pvcs.list.useQuery({ clusterId })
- trpc.namespaces.list.useQuery({ clusterId })
- trpc.events.list.useQuery({ clusterId })
- trpc.pods.list.useQuery({ clusterId })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ConfigMaps, Secrets, PVCs pages</name>
  <files>
    apps/web/src/app/clusters/[id]/configmaps/page.tsx
    apps/web/src/app/clusters/[id]/secrets/page.tsx
    apps/web/src/app/clusters/[id]/pvcs/page.tsx
  </files>
  <read_first>
    - apps/web/src/app/clusters/[id]/configmaps/page.tsx
    - apps/web/src/app/clusters/[id]/secrets/page.tsx
    - apps/web/src/app/clusters/[id]/pvcs/page.tsx
    - apps/web/src/components/resource/ResourcePageScaffold.tsx
    - docs/DESIGN.md
  </read_first>
  <action>
**ConfigMaps page rewrite** (`configmaps/page.tsx`):

Keep existing interface types. Replace render with ResourcePageScaffold.
- `ConfigMapSummary`: name (bold), data key count badge, age
- `ConfigMapExpandedDetail`: Data Keys list (show key names, truncated values), Labels, Annotations tabs via DetailTabs. Do NOT display full values of configmaps for security.
- Icon: `FileText` from lucide-react
- filterFn: search by name, namespace
- searchPlaceholder: "Search configmaps..."

**Secrets page rewrite** (`secrets/page.tsx`):

Keep existing interface types. Replace render with ResourcePageScaffold.
- `SecretSummary`: name (bold), type badge (Opaque, kubernetes.io/tls, etc.), data key count, age
- `SecretExpandedDetail`: Data Keys (names only, values masked as "***"), Type, Labels, Annotations tabs via DetailTabs. NEVER display secret values.
- Icon: `Lock` from lucide-react
- filterFn: search by name, namespace, type
- searchPlaceholder: "Search secrets..."

**PVCs page rewrite** (`pvcs/page.tsx`):

Keep existing interface types. Replace render with ResourcePageScaffold.
- `PVCSummary`: name (bold), status badge (Bound=green, Pending=amber, Lost=red), capacity, storageClass, accessModes, age
- `PVCExpandedDetail`: Volume details (volumeName, capacity, storageClass), Access Modes, Conditions, Labels tabs via DetailTabs
- Icon: `HardDrive` from lucide-react
- filterFn: search by name, namespace, status, storageClass
- searchPlaceholder: "Search PVCs..."

All 3 pages: NO ExpandableTableRow. ResourcePageScaffold only. CSS vars for all colors.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/configmaps/page.tsx
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/secrets/page.tsx
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/pvcs/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/configmaps/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/secrets/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/pvcs/page.tsx
    - pnpm typecheck passes with 0 errors
  </acceptance_criteria>
  <done>ConfigMaps, Secrets, PVCs pages rewritten with namespace-grouped card layout using ResourcePageScaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Namespaces (flat), Events (namespace-grouped), and add Expand All to Pods</name>
  <files>
    apps/web/src/app/clusters/[id]/namespaces/page.tsx
    apps/web/src/app/clusters/[id]/events/page.tsx
    apps/web/src/app/clusters/[id]/pods/page.tsx
  </files>
  <read_first>
    - apps/web/src/app/clusters/[id]/namespaces/page.tsx
    - apps/web/src/app/clusters/[id]/events/page.tsx
    - apps/web/src/app/clusters/[id]/pods/page.tsx (full file — gold standard, being enhanced)
    - apps/web/src/components/resource/ResourcePageScaffold.tsx
    - apps/web/src/components/resource/SearchFilterBar.tsx
    - docs/DESIGN.md
  </read_first>
  <action>
**Namespaces page rewrite** (`namespaces/page.tsx`):

Special case per D-02: flat card list (namespaces ARE the namespace, no grouping).
- Use ResourcePageScaffold with `flatList={true}`
- `NamespaceSummary`: name (bold), status badge (Active=green, Terminating=red), age
- `NamespaceExpandedDetail`: Status, Labels, Annotations, Resource Quotas (if any) tabs via DetailTabs
- Icon: `FolderOpen` from lucide-react
- `getNamespace`: return `'all'` (dummy — flatList skips grouping)
- filterFn: search by name, status
- searchPlaceholder: "Search namespaces..."

**Events page rewrite** (`events/page.tsx`):

Special case per D-02: group by `involvedObject.namespace`, cluster-wide events under "cluster" group.
- Use ResourcePageScaffold with `getNamespace` returning `event.involvedObject?.namespace ?? 'cluster'`
- `EventSummary`: type badge (Normal=blue, Warning=amber), reason, message (truncated), involved object (kind/name), count, age
- `EventExpandedDetail`: Full message, Involved Object details, Source, First/Last seen, Count tabs via DetailTabs
- Icon: `Activity` from lucide-react
- filterFn: search by reason, message, involvedObject name, type
- searchPlaceholder: "Search events..."

**Pods page enhancement** (`pods/page.tsx`):

Do NOT rewrite the entire page to use ResourcePageScaffold (it's already the gold standard). Only add the Expand All / Collapse All toggle per D-03:

1. Add `expandAll` state: `const [expandAll, setExpandAll] = useState(false)`
2. Import `SearchFilterBar` from `@/components/resource` and replace the inline search input section with:
```typescript
<SearchFilterBar
  searchQuery={searchQuery}
  onSearchChange={setSearchQuery}
  totalCount={pods.length}
  filteredCount={filteredPods.length}
  expandAll={expandAll}
  onExpandAllToggle={() => setExpandAll(prev => !prev)}
  searchPlaceholder="Search pods..."
/>
```
3. Pass `expanded={expandAll}` to each ExpandableCard in the pod list.
4. Keep ALL existing pod-specific rendering, delete modal, admin checks, etc. unchanged.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'flatList' apps/web/src/app/clusters/\\[id\\]/namespaces/page.tsx
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/namespaces/page.tsx
    - grep -q 'involvedObject' apps/web/src/app/clusters/\\[id\\]/events/page.tsx
    - grep -q 'ResourcePageScaffold' apps/web/src/app/clusters/\\[id\\]/events/page.tsx
    - grep -q 'expandAll' apps/web/src/app/clusters/\\[id\\]/pods/page.tsx
    - grep -q 'SearchFilterBar' apps/web/src/app/clusters/\\[id\\]/pods/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/namespaces/page.tsx
    - grep -v 'ExpandableTableRow' apps/web/src/app/clusters/\\[id\\]/events/page.tsx
    - pnpm typecheck passes with 0 errors
    - pnpm build passes with 0 errors
  </acceptance_criteria>
  <done>Namespaces (flat list), Events (involvedObject.namespace grouped), and Pods (expand-all added) pages all done. All 6 pages in this plan compile and build.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm build` both pass
- ConfigMaps, Secrets, PVCs use ResourcePageScaffold with namespace grouping
- Namespaces uses ResourcePageScaffold with `flatList={true}`
- Events uses ResourcePageScaffold with `getNamespace` returning involvedObject.namespace
- Pods page has `expandAll` state and `SearchFilterBar` component
- None of the redesigned pages import ExpandableTableRow
</verification>

<success_criteria>
All remaining resource tabs redesigned. Namespaces uses flat list. Events groups by involvedObject namespace. Pods page has expand-all toggle. pnpm build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-resource-explorer-ux-overhaul/08-05-SUMMARY.md`
</output>
