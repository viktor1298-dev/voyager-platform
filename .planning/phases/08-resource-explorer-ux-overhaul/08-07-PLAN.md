---
phase: 08-resource-explorer-ux-overhaul
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/clusters/[id]/nodes/page.tsx
autonomous: true
requirements: [UX-16]

must_haves:
  truths:
    - "Nodes page CPU/Memory bars are visible in both light and dark themes"
    - "Nodes page has proper spacing between table rows and sections"
    - "Nodes page visual hierarchy is clear in light mode (headers, labels, values distinguishable)"
  artifacts:
    - path: "apps/web/src/app/clusters/[id]/nodes/page.tsx"
      provides: "Nodes page with light-mode fixes"
      contains: "nodeStatusColor"
  key_links:
    - from: "nodes/page.tsx"
      to: "ResourceBar"
      via: "import from @/components/expandable"
      pattern: "ResourceBar"
---

<objective>
Fix the Nodes page light-mode visibility issues: CPU/Memory bars invisible in light theme, poor spacing between elements, and weak visual hierarchy. This is a UI polish fix only — NOT a full redesign to card layout (per D-02).

Purpose: The Nodes page has a unique table design appropriate for its data density. The light-mode issues make it unusable in light theme. Fix colors, spacing, and visual hierarchy without changing the page structure.
Output: Modified nodes/page.tsx with light-mode-compatible colors and improved spacing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-resource-explorer-ux-overhaul/08-CONTEXT.md

<interfaces>
From apps/web/src/components/expandable/ResourceBar.tsx:
```typescript
// ResourceBar renders CPU/Memory progress bars
// Already used in nodes page — fix its color usage, not the component itself
```

From apps/web/src/lib/status-utils.ts:
```typescript
export function nodeStatusColor(status: string): string
// Returns CSS color for node status badge
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Nodes page light-mode visibility, spacing, and visual hierarchy</name>
  <files>apps/web/src/app/clusters/[id]/nodes/page.tsx</files>
  <read_first>
    - apps/web/src/app/clusters/[id]/nodes/page.tsx (full file)
    - apps/web/src/components/expandable/ResourceBar.tsx
    - apps/web/src/lib/status-utils.ts
    - docs/DESIGN.md
    - apps/web/src/app/globals.css (check available CSS custom properties)
  </read_first>
  <action>
Read the full nodes/page.tsx and fix these specific issues:

**1. CPU/Memory bar visibility in light mode:**
- Find all instances where bar colors use low-opacity values that disappear in light mode (e.g., `bg-white/20`, `bg-white/10`, or hardcoded dark-only colors)
- Replace with CSS custom properties that work in both themes: `bg-[var(--color-chart-cpu)]` for CPU, `bg-[var(--color-chart-mem)]` for Memory
- The bar track (background) should use `bg-[var(--color-bg-tertiary)]` or `bg-[var(--color-border)]` which is visible in both themes
- If ResourceBar component is used, ensure the colors passed to it are theme-aware

**2. Spacing improvements:**
- Add `gap-3` or `gap-4` between table rows (if using flex/grid) or `py-3` for table row padding
- Ensure the expanded detail section has `p-4` padding, not crammed
- Add `mb-2` spacing between info sections in the expanded detail area
- Section headers in detail area should have `mt-4 mb-2` spacing

**3. Visual hierarchy in light mode:**
- Table headers: ensure they use `text-[var(--color-text-muted)]` (not `text-white/50` which is invisible in light)
- Node name: use `font-semibold text-[var(--color-text-primary)]`
- Status badges: ensure background uses `/15` opacity of the status color (visible in both themes)
- Labels/values: labels use `text-[var(--color-text-muted)]`, values use `text-[var(--color-text-primary)]`
- Border separators: use `border-[var(--color-border)]` (not hardcoded white/dark colors)

**4. General cleanup:**
- Replace any `text-white`, `text-white/XX`, `bg-white/XX` classes with CSS custom property equivalents
- Ensure all color references use `var(--color-*)` pattern for theme compatibility
- Keep the ExpandableTableRow structure — this page stays as a table, not cards

Do NOT change the page structure, data fetching, or functionality. Only fix visual styling for light-mode compatibility and improve spacing.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The nodes page does NOT contain 'text-white/' or 'bg-white/' classes for data display elements (search bars, badges allowed)
    - grep -q 'var(--color-' apps/web/src/app/clusters/\\[id\\]/nodes/page.tsx (uses CSS vars for colors)
    - The page still uses ExpandableTableRow (NOT converted to cards)
    - The page still imports from @/components/expandable (ResourceBar, etc.)
    - pnpm typecheck passes with 0 errors
    - pnpm build passes with 0 errors
  </acceptance_criteria>
  <done>Nodes page CPU/Memory bars visible in light mode, spacing improved, visual hierarchy clear in both themes. Structure unchanged — still table layout with ExpandableTableRow.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm build` both pass
- Nodes page uses CSS custom properties for all data display colors
- No `text-white/50`, `bg-white/10` etc. in data display elements
- Page structure unchanged (still table-based with ExpandableTableRow)
</verification>

<success_criteria>
Nodes page is fully usable in light mode: bars visible, text readable, proper spacing and visual hierarchy. pnpm build passes. No structural changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-resource-explorer-ux-overhaul/08-07-SUMMARY.md`
</output>
