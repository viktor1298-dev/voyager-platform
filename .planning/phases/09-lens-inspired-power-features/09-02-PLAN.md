---
phase: 09-lens-inspired-power-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/clusters/cluster-tabs-config.ts
  - apps/web/src/components/resource/ActionToolbar.tsx
  - apps/web/src/components/resource/DeleteConfirmDialog.tsx
  - apps/web/src/components/resource/RestartConfirmDialog.tsx
  - apps/web/src/components/resource/ScaleInput.tsx
  - apps/web/src/components/resource/PortForwardCopy.tsx
  - apps/web/src/components/expandable/DetailTabs.tsx
autonomous: true
requirements:
  - LENS-08
  - LENS-13

must_haves:
  truths:
    - "GroupedTabBar shows new 'Cluster Ops' group with Helm, CRDs, RBAC entries and Network Policies under Networking, Resource Quotas under Config"
    - "ActionToolbar renders right-aligned action buttons inside expanded ExpandableCard detail panels"
    - "Delete confirmation requires typing the exact resource name to enable the confirm button"
    - "Restart confirmation shows pod impact count"
    - "Scale input shows current->new replica count inline"
    - "Port forward button shows a popover with copyable kubectl port-forward command"
  artifacts:
    - path: "apps/web/src/components/resource/ActionToolbar.tsx"
      provides: "Right-aligned action buttons for ExpandableCard detail panels"
      exports: ["ActionToolbar"]
    - path: "apps/web/src/components/resource/DeleteConfirmDialog.tsx"
      provides: "GitHub-style type-name-to-confirm delete dialog"
      exports: ["DeleteConfirmDialog"]
    - path: "apps/web/src/components/resource/RestartConfirmDialog.tsx"
      provides: "Impact-aware restart confirmation dialog"
      exports: ["RestartConfirmDialog"]
    - path: "apps/web/src/components/resource/ScaleInput.tsx"
      provides: "Inline replica count input with Apply button"
      exports: ["ScaleInput"]
    - path: "apps/web/src/components/resource/PortForwardCopy.tsx"
      provides: "kubectl port-forward command copy popover"
      exports: ["PortForwardCopy"]
    - path: "apps/web/src/components/clusters/cluster-tabs-config.ts"
      provides: "Extended tab config with Cluster Ops group"
      contains: "cluster-ops"
  key_links:
    - from: "apps/web/src/components/expandable/DetailTabs.tsx"
      to: "apps/web/src/components/resource/ActionToolbar.tsx"
      via: "actions prop on DetailTabs"
      pattern: "actions.*ActionToolbar"
---

<objective>
Extend GroupedTabBar with new tab groups (Cluster Ops, Network Policies, Resource Quotas), create the ActionToolbar component with all confirmation dialogs, and wire the actions prop into DetailTabs.

Purpose: Navigation infrastructure for all new pages and reusable action components for workload management across all resource types.
Output: Extended tab navigation, ActionToolbar, DeleteConfirmDialog, RestartConfirmDialog, ScaleInput, PortForwardCopy — all reusable components ready for integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lens-inspired-power-features/09-CONTEXT.md
@.planning/phases/09-lens-inspired-power-features/09-UI-SPEC.md

@apps/web/src/components/clusters/cluster-tabs-config.ts
@apps/web/src/components/clusters/GroupedTabBar.tsx
@apps/web/src/components/expandable/DetailTabs.tsx
@apps/web/src/lib/animation-constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GroupedTabBar config and create ActionToolbar with PortForwardCopy</name>
  <files>apps/web/src/components/clusters/cluster-tabs-config.ts, apps/web/src/components/resource/ActionToolbar.tsx, apps/web/src/components/resource/PortForwardCopy.tsx, apps/web/src/components/expandable/DetailTabs.tsx</files>
  <read_first>
    - apps/web/src/components/clusters/cluster-tabs-config.ts (current tab entries and types)
    - apps/web/src/components/clusters/GroupedTabBar.tsx (how tab groups render)
    - apps/web/src/components/expandable/DetailTabs.tsx (current props interface, to add actions prop)
    - apps/web/src/lib/animation-constants.ts (B-style animation constants for dialogs)
    - docs/DESIGN.md (animation standards)
  </read_first>
  <action>
**Extend `cluster-tabs-config.ts`** (per D-01):

Add `Wrench` to lucide-react imports. Add new entries to `CLUSTER_TAB_ENTRIES` array:

1. Add `{ id: 'network-policies', label: 'Network Policies', path: '/network-policies' }` to the `networking` group children
2. Add `{ id: 'resource-quotas', label: 'Resource Quotas', path: '/resource-quotas' }` to the `config` group children
3. Add new group after `scaling`:
```typescript
{
  type: 'group',
  id: 'cluster-ops',
  label: 'Cluster Ops',
  icon: Wrench,
  children: [
    { id: 'helm', label: 'Helm', path: '/helm' },
    { id: 'crds', label: 'CRDs', path: '/crds' },
    { id: 'rbac', label: 'RBAC', path: '/rbac' },
  ],
},
```

**Create `ActionToolbar.tsx`** (per D-05, I-04):

```typescript
interface ActionToolbarProps {
  actions: ActionButton[]
}

interface ActionButton {
  id: string
  label: string
  icon: ElementType // lucide icon component
  variant: 'default' | 'accent' | 'destructive'
  onClick: () => void
  disabled?: boolean
}
```

- Render as a `div` with `flex items-center gap-1` layout
- Buttons: ghost variant, 32px height (`h-8`), 12px icon + 12px label text, `gap-1`
- Color variants: `default` = text-muted-foreground, `accent` = text-[var(--color-accent)] or accent color, `destructive` = text-destructive
- Hover: `transition-colors duration-150`
- Use lucide icons: `Terminal` (Exec), `RotateCw` (Restart), `Scaling` (Scale), `Trash2` (Delete), `ExternalLink` (Port Forward)

**Create `PortForwardCopy.tsx`** (per D-15, I-13):

Props: `{ podName: string, namespace: string, containerPorts: { name?: string, containerPort: number }[], clusterContext: string }`

- Render as Popover (shadcn) triggered by the "Port Forward" action button
- Popover content: heading "Port Forward Command" in 12px semibold
- For each container port, show command: `kubectl port-forward pod/{podName} {containerPort}:{containerPort} -n {namespace} --context {clusterContext}`
- Copy icon (lucide `Copy`, 14px) next to each command
- On click: `navigator.clipboard.writeText(command)`, icon changes to `Check` for 2s, sonner toast "Command copied to clipboard"
- Font: 13px Geist Mono (`font-mono text-[13px]`)

**Modify `DetailTabs.tsx`** (per D-05):

Add optional `actions` prop to the DetailTabs component:
```typescript
interface DetailTabsProps {
  // ... existing props
  actions?: React.ReactNode  // Rendered right-aligned in the tab header row
}
```

In the render, add `{actions && <div className="ml-auto flex items-center">{actions}</div>}` inside the tab header row, after the tab buttons.

Run `pnpm typecheck` to verify.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - cluster-tabs-config.ts contains `id: 'cluster-ops'` with children `helm`, `crds`, `rbac`
    - cluster-tabs-config.ts networking group children includes `id: 'network-policies'`
    - cluster-tabs-config.ts config group children includes `id: 'resource-quotas'`
    - cluster-tabs-config.ts imports `Wrench` from lucide-react
    - ActionToolbar.tsx exists and exports `ActionToolbar`
    - ActionToolbar.tsx accepts `actions: ActionButton[]` prop with `variant: 'default' | 'accent' | 'destructive'`
    - PortForwardCopy.tsx exists and contains `kubectl port-forward pod/`
    - PortForwardCopy.tsx contains `navigator.clipboard.writeText`
    - DetailTabs.tsx has `actions` in its props interface
    - DetailTabs.tsx renders `{actions}` in the tab header row
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>GroupedTabBar config extended with Cluster Ops group (Helm, CRDs, RBAC), Network Policies, Resource Quotas. ActionToolbar renders configurable action buttons. PortForwardCopy generates copyable kubectl commands. DetailTabs accepts right-aligned actions.</done>
</task>

<task type="auto">
  <name>Task 2: Create confirmation dialogs — Delete, Restart, Scale</name>
  <files>apps/web/src/components/resource/DeleteConfirmDialog.tsx, apps/web/src/components/resource/RestartConfirmDialog.tsx, apps/web/src/components/resource/ScaleInput.tsx</files>
  <read_first>
    - apps/web/src/components/resource/ActionToolbar.tsx (created in Task 1 — action trigger pattern)
    - apps/web/src/lib/animation-constants.ts (dialogVariants, overlayVariants for animation)
    - apps/web/package.json (verify shadcn Dialog is available)
    - docs/DESIGN.md (B-style animation standards)
  </read_first>
  <action>
**Create `DeleteConfirmDialog.tsx`** (per D-06, I-04 Delete):

Props:
```typescript
interface DeleteConfirmDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  resourceType: string  // e.g., "Pod", "Deployment"
  resourceName: string
  namespace: string
  onConfirm: () => void
  isDeleting?: boolean
}
```

- Use shadcn `Dialog` component
- Title: "Delete {resourceType}" (per UI-SPEC copywriting)
- Body: "This action cannot be undone. This will permanently delete the {resourceType} **{resourceName}** from namespace **{namespace}**."
- Input with label: "Type **{resourceName}** to confirm"
- Input value must match `resourceName` exactly (case-sensitive) to enable confirm button
- Confirm button: "I understand, delete this {resourceType}" — red/destructive variant, disabled until name matches
- Dismiss button: "Keep {resourceType}" — ghost variant
- Dialog border: use destructive color (red-themed)
- Apply `dialogVariants` and `overlayVariants` from animation-constants for enter/exit animation
- Show loading spinner on confirm button when `isDeleting` is true

**Create `RestartConfirmDialog.tsx`** (per D-06, I-04 Restart):

Props:
```typescript
interface RestartConfirmDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  resourceType: string  // "Deployment", "StatefulSet", "DaemonSet"
  resourceName: string
  podCount: number
  onConfirm: () => void
  isRestarting?: boolean
}
```

- Title: "Restart {resourceType}"
- Body: "This will trigger a rolling restart of **{podCount} pods** managed by {resourceName}. Existing connections may be briefly interrupted."
- Confirm button: "Restart" — default variant
- Dismiss button: "No, don't restart" — ghost variant
- Apply `dialogVariants` for animation
- Show loading spinner when `isRestarting` is true

**Create `ScaleInput.tsx`** (per D-06, I-04 Scale):

Props:
```typescript
interface ScaleInputProps {
  currentReplicas: number
  onScale: (newReplicas: number) => void
  isScaling?: boolean
}
```

- Inline component (no dialog — per D-06)
- Display: "Current: {currentReplicas}" label, number input (min=0, max=100), "Update Replicas" button
- Show transition: "{currentReplicas} -> {newValue}" when input differs from current
- Button disabled when value equals current or is negative
- Show loading spinner when `isScaling` is true
- On success, show sonner toast "Scaled {resource} to {N} replicas" (toast triggered by parent)
- Compact layout: `flex items-center gap-2`, input width `w-20`

Run `pnpm typecheck && pnpm build` to verify.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - DeleteConfirmDialog.tsx exists and exports `DeleteConfirmDialog`
    - DeleteConfirmDialog.tsx contains exact copy "I understand, delete this" for confirm button
    - DeleteConfirmDialog.tsx has input validation that compares typed value to `resourceName`
    - RestartConfirmDialog.tsx exists and exports `RestartConfirmDialog`
    - RestartConfirmDialog.tsx contains "rolling restart of" in the dialog body
    - ScaleInput.tsx exists and exports `ScaleInput`
    - ScaleInput.tsx contains number input with `currentReplicas` and "Update Replicas" button text
    - All three components use `open` and `onOpenChange` props (dialogs) or controlled state (ScaleInput)
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>DeleteConfirmDialog with type-name-to-confirm pattern. RestartConfirmDialog with pod count impact. ScaleInput with inline current->new display. All animated with B-style variants. All type-safe.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with 0 errors
- `pnpm typecheck` passes with 0 errors
- GroupedTabBar config has 7 groups (original 5 + cluster-ops, updated networking/config)
- All 5 action components (ActionToolbar, DeleteConfirmDialog, RestartConfirmDialog, ScaleInput, PortForwardCopy) exist and export correctly
- DetailTabs accepts optional `actions` prop
</verification>

<success_criteria>
- Build and typecheck pass
- Tab config extended with 5 new tab entries across 3 groups
- ActionToolbar renders action buttons with correct variant styling
- Delete dialog blocks until exact name match
- Restart dialog shows pod count
- Scale input shows inline replica change
- Port forward copies kubectl command
</success_criteria>

<output>
After completion, create `.planning/phases/09-lens-inspired-power-features/09-02-SUMMARY.md`
</output>
