---
phase: 09-lens-inspired-power-features
plan: 09
type: execute
wave: 4
depends_on: ["09-01", "09-08"]
files_modified:
  - apps/api/src/routers/topology.ts
  - apps/api/src/routers/index.ts
  - apps/api/src/lib/cache-keys.ts
  - apps/web/src/components/topology/TopologyMap.tsx
  - apps/web/src/components/topology/TopologyNode.tsx
  - apps/web/src/components/topology/TopologyEdge.tsx
  - apps/web/src/app/clusters/[id]/page.tsx
  - apps/web/src/components/network/NetworkPolicyGraph.tsx
  - apps/web/src/components/network/NetworkPolicyNode.tsx
  - apps/web/src/components/network/NetworkPolicyEdge.tsx
  - apps/web/src/app/clusters/[id]/network-policies/page.tsx
autonomous: true
requirements:
  - LENS-11

must_haves:
  truths:
    - "Topology map renders cluster resources as a directed graph with Ingress->Service->Deployment->Pod->Node relationships"
    - "Every graph node is clickable and navigates to the corresponding resource tab with highlight"
    - "Network Policy graph shows pods/namespaces as nodes with policy edges (allow/deny)"
    - "Both graphs use dagre layout for automatic positioning"
    - "Both graphs have zoom, pan, and minimap"
  artifacts:
    - path: "apps/web/src/components/topology/TopologyMap.tsx"
      provides: "React Flow topology graph of cluster resources"
      exports: ["TopologyMap"]
      min_lines: 100
    - path: "apps/web/src/components/network/NetworkPolicyGraph.tsx"
      provides: "React Flow network policy visualization"
      exports: ["NetworkPolicyGraph"]
      min_lines: 80
    - path: "apps/api/src/routers/topology.ts"
      provides: "Topology data aggregation router"
      exports: ["topologyRouter"]
  key_links:
    - from: "apps/web/src/components/topology/TopologyMap.tsx"
      to: "apps/api/src/routers/topology.ts"
      via: "tRPC topology.graph query"
      pattern: "trpc\\.topology\\.graph"
    - from: "apps/web/src/components/topology/TopologyNode.tsx"
      to: "apps/web/src/components/resource/RelatedResourceLink.tsx"
      via: "Node click navigates to resource tab"
      pattern: "RelatedResourceLink|router\\.push"
---

<objective>
Build the Resource Topology Map and Network Policy Graph visualizations using React Flow with dagre layout.

Purpose: LENS-09 topology (D-09) and LENS-11 network policy graph (D-10) — the differentiator visual features that make Voyager stand out from basic dashboards.
Output: Interactive topology graph on Overview, network policy graph on Network Policies page, both with clickable nodes and automatic layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lens-inspired-power-features/09-CONTEXT.md
@.planning/phases/09-lens-inspired-power-features/09-RESEARCH.md
@.planning/phases/09-lens-inspired-power-features/09-UI-SPEC.md
@.planning/phases/09-lens-inspired-power-features/09-01-SUMMARY.md
@.planning/phases/09-lens-inspired-power-features/09-08-SUMMARY.md

@apps/web/src/components/resource/RelatedResourceLink.tsx
@apps/web/src/app/clusters/[id]/page.tsx
@apps/web/src/app/clusters/[id]/network-policies/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create topology backend router and TopologyMap frontend component</name>
  <files>apps/api/src/routers/topology.ts, apps/api/src/routers/index.ts, apps/api/src/lib/cache-keys.ts, apps/web/src/components/topology/TopologyMap.tsx, apps/web/src/components/topology/TopologyNode.tsx, apps/web/src/components/topology/TopologyEdge.tsx, apps/web/src/app/clusters/[id]/page.tsx</files>
  <read_first>
    - apps/api/src/routers/pods.ts (K8s router pattern)
    - apps/api/src/routers/index.ts (registry)
    - apps/web/src/app/clusters/[id]/page.tsx (Overview page — where topology map will be added)
    - apps/web/src/components/resource/RelatedResourceLink.tsx (hyperlink navigation pattern for node clicks)
    - apps/web/src/app/globals.css (--color-graph-* tokens)
    - apps/web/src/lib/animation-constants.ts (graphNodeVariants)
  </read_first>
  <action>
**Create `apps/api/src/routers/topology.ts`**:

Export `topologyRouter` with procedure:

1. `graph`: `protectedProcedure`, input `{ clusterId, namespace: z.string().optional() }`
   - Fetch all relevant resources in parallel:
     ```typescript
     const [ingresses, services, deployments, statefulSets, daemonSets, pods, nodes] = await Promise.all([
       networkingApi.listIngressForAllNamespaces(),
       coreV1.listServiceForAllNamespaces(),
       appsV1.listDeploymentForAllNamespaces(),
       appsV1.listStatefulSetForAllNamespaces(),
       appsV1.listDaemonSetForAllNamespaces(),
       coreV1.listPodForAllNamespaces(),
       coreV1.listNode(),
     ])
     ```
   - If `namespace` provided, filter all resources to that namespace (except Nodes which are cluster-scoped)
   - Build graph structure:
     - **Nodes**: Each resource becomes a graph node with `{ id, type (ingress|service|deployment|statefulset|daemonset|pod|node), name, namespace, status, resourceCount? }`
     - **Edges**: Build relationships:
       - Ingress -> Service (from ingress rules backend serviceName)
       - Service -> Pods/Deployments (from service selector matching)
       - Deployment/StatefulSet/DaemonSet -> Pods (from label selector matching)
       - Pod -> Node (from pod.spec.nodeName)
   - Limit: Return max 200 nodes total (Pitfall 4). If more, group pods by deployment (show deployment as single node with pod count badge).
   - Cache with `CACHE_KEYS.k8sTopology(clusterId)` at 15s TTL
   - Return: `{ nodes: TopologyNode[], edges: TopologyEdge[] }`

**Register** in index.ts: `topology: topologyRouter`

**Add cache key**: `k8sTopology: (clusterId: string) => \`k8s:topology:${clusterId}\``

**Create `apps/web/src/components/topology/TopologyNode.tsx`** (per I-07):

Custom React Flow node component (per D-09):
- Props: React Flow `NodeProps` with custom data
- Node shapes by type (per I-07): Use CSS border-radius to simulate shapes — all rendered as `div` elements:
  - Ingress: clip-path hexagon shape
  - Service: clip-path diamond (rotated square)
  - Deployment/StatefulSet/DaemonSet: rounded rectangle (default)
  - Pod: fully rounded (rounded-full)
  - Node: large rounded rectangle
- Size: width 160px, height 48px minimum
- Content: resource name (12px semibold, truncated), status indicator dot (8px), resource count badge
- Border: 2px solid, color from `--color-graph-node-healthy/warning/error` based on status
- Background: `var(--color-bg-card)`
- Hover: border 3px, subtle glow matching health color
- **Click handler**: Navigate to resource tab with highlight. Use `router.push(\`/clusters/${clusterId}/${resourceType}?highlight=${resourceName}\`)` pattern from `RelatedResourceLink`

**CRITICAL (from user requirement)**: Every node MUST be clickable and hyperlink to the actual resource.

**Create `apps/web/src/components/topology/TopologyEdge.tsx`**:

Custom React Flow edge with:
- Bezier curves, 1.5px stroke
- Default color: `var(--color-graph-edge)`
- Active/selected: `var(--color-graph-edge-active)`
- Arrow at target end, 8px arrow size
- Animate stroke-dashoffset from 100% to 0 over 600ms on initial render

**Create `apps/web/src/components/topology/TopologyMap.tsx`** (per I-07, RESEARCH Pattern 6):

- Import `ReactFlow`, `Controls`, `MiniMap`, `Background` from `@xyflow/react`
- Import `@xyflow/react/dist/style.css`
- Import dagre from `@dagrejs/dagre`
- Use `trpc.topology.graph.useQuery({ clusterId, namespace })` for data
- Apply dagre layout (per RESEARCH Pattern 6):
  ```typescript
  const g = new dagre.graphlib.Graph().setDefaultEdgeLabel(() => ({}))
  g.setGraph({ rankdir: 'LR', ranksep: 100, nodesep: 60 })
  // Add nodes and edges, run dagre.layout(g), map positions
  ```
- Define `nodeTypes` as stable reference OUTSIDE component (Pitfall 4):
  ```typescript
  const nodeTypes = { topology: TopologyNode }
  ```
- Same for `edgeTypes`
- Namespace grouping: dashed border rectangle around namespace (use React Flow's Group node type or custom CSS)
- Zoom/pan: React Flow built-in
- Minimap: bottom-right, 150x100px
- Filter: namespace dropdown and resource name search input
- Loading: skeleton graph (5 placeholder nodes with shimmer)
- Empty: "No resources found in this cluster."
- Apply `graphNodeVariants` for staggered node entrance (50ms per node, capped at 300ms)

**Add TopologyMap to Overview page** (`clusters/[id]/page.tsx`):

Add a "Resource Topology" section with a heading and the `TopologyMap` component. Give it substantial height (~500px) or a dedicated section. Add a toggle or button to show/hide if space is constrained.

Run `pnpm typecheck && pnpm build` to verify.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/routers/topology.ts exists and exports `topologyRouter`
    - topology.ts fetches ingresses, services, deployments, pods, nodes in parallel
    - topology.ts builds nodes and edges arrays with relationship mapping
    - topology.ts limits to max 200 nodes
    - index.ts contains `topology: topologyRouter`
    - TopologyMap.tsx imports from `@xyflow/react` and `@dagrejs/dagre`
    - TopologyMap.tsx calls `trpc.topology.graph.useQuery`
    - TopologyMap.tsx defines `nodeTypes` outside the component (stable reference)
    - TopologyNode.tsx has click handler that navigates to resource tab
    - TopologyEdge.tsx has bezier curve with arrow
    - Overview page.tsx renders `TopologyMap` component
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Topology map renders Ingress->Service->Deployment->Pod->Node graph with dagre layout. Every node clickable, navigates to resource tab. Minimap, zoom/pan, namespace filtering. Added to Overview page.</done>
</task>

<task type="auto">
  <name>Task 2: Create NetworkPolicyGraph and integrate with Network Policies page</name>
  <files>apps/web/src/components/network/NetworkPolicyGraph.tsx, apps/web/src/components/network/NetworkPolicyNode.tsx, apps/web/src/components/network/NetworkPolicyEdge.tsx, apps/web/src/app/clusters/[id]/network-policies/page.tsx</files>
  <read_first>
    - apps/web/src/components/topology/TopologyMap.tsx (Task 1 — reuse React Flow + dagre pattern)
    - apps/web/src/components/topology/TopologyNode.tsx (custom node pattern)
    - apps/web/src/app/clusters/[id]/network-policies/page.tsx (Plan 08 — page to enhance with graph toggle)
    - apps/web/src/app/globals.css (--color-netpol-allow, --color-netpol-deny tokens)
  </read_first>
  <action>
**Create `apps/web/src/components/network/NetworkPolicyNode.tsx`** (per I-08):

Custom React Flow node:
- Pod/Deployment: circle, 40px diameter
- Namespace: rounded rectangle with dashed border
- Label: resource name, 12px semibold
- Color: neutral by default, highlighted with accent when selected

**Create `apps/web/src/components/network/NetworkPolicyEdge.tsx`** (per I-08):

Custom React Flow edge:
- Allow rules: solid arrow, `var(--color-netpol-allow)`, direction indicates ingress/egress
- Deny rules: dashed line with X marker, `var(--color-netpol-deny)`
- Animated entrance: stroke-dashoffset animation 600ms

**Create `apps/web/src/components/network/NetworkPolicyGraph.tsx`** (per D-10, I-08):

- Use `trpc.networkPolicies.list.useQuery({ clusterId })` for data
- Transform network policy data into React Flow nodes and edges:
  - Each pod selector becomes a node
  - Each namespace becomes a group node
  - Each ingress/egress rule becomes a directed edge
  - Edge style depends on rule type (allow=solid green, deny=dashed red)
- Dagre layout: left-to-right (`rankdir: 'LR'`)
- Namespace filter dropdown, policy name search
- Pod click: highlight all policies affecting that pod (filter edges)
- Policy edge click: popover showing rule details (ports, protocols, selectors)
- Empty: "No network policies found. All traffic is allowed by default."
- Use shared `nodeTypes`/`edgeTypes` pattern (defined outside component)
- React Flow `Controls` and `MiniMap` included

**Integrate with Network Policies page**:

Add a toggle at the top of the network-policies page: `[Graph | List]` segmented control (similar to Events timeline/cards toggle pattern from D-08)
- Graph view: render `NetworkPolicyGraph`
- List view: existing ExpandableCard list from Plan 08
- Default: List view

Run `pnpm typecheck && pnpm build` to verify.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - NetworkPolicyGraph.tsx exists and imports from `@xyflow/react`
    - NetworkPolicyGraph.tsx transforms network policy data into nodes and edges
    - NetworkPolicyNode.tsx exists with circle shape for pods
    - NetworkPolicyEdge.tsx exists with allow (solid green) and deny (dashed red) styling
    - NetworkPolicyEdge.tsx uses `--color-netpol-allow` and `--color-netpol-deny` colors
    - network-policies/page.tsx has toggle between Graph and List views
    - NetworkPolicyGraph.tsx includes `Controls` and `MiniMap` from React Flow
    - NetworkPolicyGraph.tsx uses dagre layout with `rankdir: 'LR'`
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Network policy graph visualizes policies as directed graph with allow/deny edges. Toggle between graph and list views on Network Policies page. Shared React Flow pattern with topology map.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with 0 errors
- `pnpm typecheck` passes with 0 errors
- Topology map renders on Overview page with interactive nodes
- Network policy graph renders with allow/deny edge styling
- Both graphs use dagre layout for automatic positioning
</verification>

<success_criteria>
- Build and typecheck pass
- Topology map shows resource relationships (Ingress->Service->Pod->Node)
- Every topology node is clickable and navigates to resource tab
- Network policy graph shows allow/deny traffic flows
- Both graphs have zoom, pan, minimap, namespace filtering
- Dagre layout produces readable graph positioning
</success_criteria>

<output>
After completion, create `.planning/phases/09-lens-inspired-power-features/09-09-SUMMARY.md`
</output>
