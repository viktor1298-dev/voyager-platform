---
phase: 09-lens-inspired-power-features
plan: 10
type: execute
wave: 4
depends_on: ["09-01"]
files_modified:
  - apps/web/src/components/events/EventsTimeline.tsx
  - apps/web/src/components/events/TimelineSwimLane.tsx
  - apps/web/src/components/events/TimelineEventDot.tsx
  - apps/web/src/app/clusters/[id]/events/page.tsx
  - apps/web/src/app/clusters/[id]/pods/page.tsx
autonomous: true
requirements:
  - LENS-06

must_haves:
  truths:
    - "Events page has a toggle between Timeline and Cards views"
    - "Timeline view shows horizontal swim lanes grouped by resource type"
    - "Event dots are color-coded by type: Normal=blue, Warning=amber, Error=red"
    - "Hovering an event dot shows a popover with full event details"
    - "Existing Cards view is preserved and remains the default"
  artifacts:
    - path: "apps/web/src/components/events/EventsTimeline.tsx"
      provides: "Horizontal swim lane timeline visualization"
      exports: ["EventsTimeline"]
      min_lines: 100
    - path: "apps/web/src/components/events/TimelineSwimLane.tsx"
      provides: "Single resource type swim lane row"
      exports: ["TimelineSwimLane"]
    - path: "apps/web/src/components/events/TimelineEventDot.tsx"
      provides: "Color-coded event dot with hover popover"
      exports: ["TimelineEventDot"]
  key_links:
    - from: "apps/web/src/components/events/EventsTimeline.tsx"
      to: "apps/web/src/app/clusters/[id]/events/page.tsx"
      via: "Timeline view toggle in events page"
      pattern: "EventsTimeline"
---

<objective>
Build the Events Timeline horizontal swim lane visualization and wire the Exec button on Pods page to the terminal drawer.

Purpose: LENS-06 (Events Timeline) — upgrade the Events tab with an interactive timeline visualization alongside the existing card view. Also complete LENS-01 by wiring the Pod Exec button to the terminal drawer from Plan 05.
Output: EventsTimeline component with swim lanes, event dots, popover details. Pods page Exec button opens terminal drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lens-inspired-power-features/09-CONTEXT.md
@.planning/phases/09-lens-inspired-power-features/09-UI-SPEC.md
@.planning/phases/09-lens-inspired-power-features/09-01-SUMMARY.md

@apps/web/src/app/clusters/[id]/events/page.tsx
@apps/web/src/app/clusters/[id]/pods/page.tsx
@apps/web/src/components/terminal/terminal-context.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EventsTimeline with swim lanes and integrate into Events page</name>
  <files>apps/web/src/components/events/EventsTimeline.tsx, apps/web/src/components/events/TimelineSwimLane.tsx, apps/web/src/components/events/TimelineEventDot.tsx, apps/web/src/app/clusters/[id]/events/page.tsx</files>
  <read_first>
    - apps/web/src/app/clusters/[id]/events/page.tsx (MUST read — current events page implementation, data source, to add timeline toggle)
    - apps/web/src/app/globals.css (--color-timeline-normal, --color-timeline-warning, --color-timeline-error tokens)
    - apps/web/src/lib/animation-constants.ts (swimLaneVariants for entrance animation)
    - docs/DESIGN.md (B-style animation standards)
  </read_first>
  <action>
**Create `apps/web/src/components/events/TimelineEventDot.tsx`** (per I-06):

Props: `{ event: K8sEvent, timelineStart: number, timelineEnd: number, laneWidth: number }`

- Render as 8px diameter circle
- Color by event type using CSS vars:
  - Normal: `var(--color-timeline-normal)`
  - Warning: `var(--color-timeline-warning)`
  - Error (type contains "Error" or "Failed"): `var(--color-timeline-error)`
- Position: calculate `left` percentage based on event timestamp relative to timeline range
  - `left = ((eventTime - timelineStart) / (timelineEnd - timelineStart)) * laneWidth`
- Hover: Show shadcn `Popover` or `Tooltip` with:
  - Event type (Normal/Warning)
  - Reason
  - Message (truncated at 200 chars)
  - First seen / Last seen
  - Count
  - Involved object (resource kind + name)
- Apply opacity 0.8 default, 1.0 on hover

**Create `apps/web/src/components/events/TimelineSwimLane.tsx`** (per I-06):

Props: `{ resourceType: string, events: K8sEvent[], timelineStart: number, timelineEnd: number, isCollapsed: boolean, onToggle: () => void }`

- Render as horizontal row with:
  - Left label column (120px width): resource type name (12px semibold), clickable to collapse/expand
  - Right lane area: render `TimelineEventDot` for each event, positioned by timestamp
- Collapsed: hide dots, show compact row
- Apply `swimLaneVariants` for entrance animation

**Create `apps/web/src/components/events/EventsTimeline.tsx`** (per D-08, I-06):

Props: `{ events: K8sEvent[] }`

- **Time axis**: Horizontal bar at top, labels in 12px Geist Mono
  - Calculate time range from min/max event timestamps (with padding)
  - Render tick marks at appropriate intervals (auto-scale: 1min, 5min, 15min, 1h depending on range)
  - Format: `HH:MM:SS` for ranges < 1h, `HH:MM` for ranges 1-24h, `Mon Day` for > 24h
- **Swim lanes**: Group events by involved object kind (Pod, Deployment, Node, ReplicaSet, etc.)
  - Only show swim lanes for resource types that have events
  - Each lane gets a `TimelineSwimLane`
  - Lanes sorted by event count (most active first)
- **Lane toggle**: Click lane label to collapse/expand
- **Zoom**: Implement drag-to-select time region zoom:
  - `onMouseDown` starts selection, `onMouseMove` draws selection rectangle, `onMouseUp` zooms to selected range
  - Reset button to restore original time range
- **Scroll**: Horizontal scroll for time axis (overflow-x-auto), vertical scroll for many swim lanes (overflow-y-auto, max-height 600px)
- **Entrance animation**: Dots fade in with stagger (30ms per dot, capped at 300ms total)
- Empty: "No events in the selected time range."

**Integrate into Events page** (per D-08):

Add a segmented control toggle at the top of the events page:
- `[Timeline | Cards]` buttons (use existing `data-state="active"` tab pattern or simple toggle)
- Default: Cards (preserves existing behavior)
- When "Timeline" selected: render `<EventsTimeline events={events} />`
- When "Cards" selected: render existing card/table view
- Pass the same events data to both views

Run `pnpm typecheck && pnpm build` to verify.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - EventsTimeline.tsx exists and exports `EventsTimeline`
    - EventsTimeline.tsx groups events by resource type into swim lanes
    - EventsTimeline.tsx has time axis with tick marks and labels
    - EventsTimeline.tsx has drag-to-zoom functionality
    - TimelineSwimLane.tsx exists with collapsible lane and 120px label column
    - TimelineEventDot.tsx exists with 8px circle using `--color-timeline-normal/warning/error` colors
    - TimelineEventDot.tsx has Popover/Tooltip with event details on hover
    - events/page.tsx has toggle between "Timeline" and "Cards" views
    - events/page.tsx defaults to "Cards" view
    - Existing card view is preserved when "Cards" is selected
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Events Timeline visualization with horizontal swim lanes, color-coded event dots, drag-to-zoom, lane collapse toggle. Integrated into Events page alongside existing Cards view with toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Pod Exec button to terminal drawer and final integration touches</name>
  <files>apps/web/src/app/clusters/[id]/pods/page.tsx</files>
  <read_first>
    - apps/web/src/app/clusters/[id]/pods/page.tsx (current state after Plan 04 — has Exec button as noop)
    - apps/web/src/components/terminal/terminal-context.tsx (Plan 05 — openTerminal method)
  </read_first>
  <action>
**Wire the Exec button on Pods page** to the terminal drawer:

In `pods/page.tsx`:
1. Import `useTerminal` from `@/components/terminal/terminal-context`
2. Get `openTerminal` from `const { openTerminal } = useTerminal()`
3. Find the Exec action button in the ActionToolbar (currently a noop from Plan 04)
4. Replace the noop onClick with:
   ```typescript
   onClick: () => openTerminal({
     podName: pod.name,
     container: pod.containers?.[0]?.name ?? pod.name,
     namespace: pod.namespace,
     clusterId,
   })
   ```
5. If pod has multiple containers, the terminal drawer's container dropdown (from Plan 05) handles selection. Pass the first container as default.

**Container selection for Exec**: If the pod data includes `containers` array:
- Use first container as default for `openTerminal` call
- The TerminalDrawer container dropdown (Plan 05) allows switching containers within the session

Run `pnpm typecheck && pnpm build` to verify.
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - pods/page.tsx imports `useTerminal` from terminal-context
    - pods/page.tsx calls `openTerminal({ podName, container, namespace, clusterId })` in Exec button onClick
    - Exec button onClick is no longer a noop
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Exec button on Pods page opens terminal drawer with the selected pod. Full pod terminal flow complete: click Exec -> drawer opens -> xterm.js connects via WebSocket -> K8s exec session active.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with 0 errors
- `pnpm typecheck` passes with 0 errors
- Events page toggle between Timeline and Cards works
- Timeline shows color-coded event dots in swim lanes
- Exec button on Pods opens terminal drawer
</verification>

<success_criteria>
- Build and typecheck pass
- Events Timeline shows swim lanes by resource type
- Event dots colored by type (Normal/Warning/Error)
- Hover popover shows full event details
- Drag-to-zoom works on timeline
- Pods Exec button opens terminal drawer with correct pod/container/namespace
</success_criteria>

<output>
After completion, create `.planning/phases/09-lens-inspired-power-features/09-10-SUMMARY.md`
</output>
