---
phase: 10-lens-style-live-data-k8s-watch-stream-architecture
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/jobs/health-sync.ts
  - apps/api/src/jobs/node-sync.ts
  - apps/api/src/jobs/event-sync.ts
  - apps/api/src/lib/cache-keys.ts
  - apps/web/src/app/clusters/[id]/pods/page.tsx
  - apps/web/src/app/clusters/[id]/deployments/page.tsx
  - apps/web/src/app/clusters/[id]/services/page.tsx
  - apps/web/src/app/clusters/[id]/ingresses/page.tsx
  - apps/web/src/app/clusters/[id]/statefulsets/page.tsx
  - apps/web/src/app/clusters/[id]/daemonsets/page.tsx
  - apps/web/src/app/clusters/[id]/jobs/page.tsx
  - apps/web/src/app/clusters/[id]/cronjobs/page.tsx
  - apps/web/src/app/clusters/[id]/hpa/page.tsx
  - apps/web/src/app/clusters/[id]/configmaps/page.tsx
  - apps/web/src/app/clusters/[id]/secrets/page.tsx
  - apps/web/src/app/clusters/[id]/pvcs/page.tsx
  - apps/web/src/app/clusters/[id]/namespaces/page.tsx
  - apps/web/src/app/clusters/[id]/nodes/page.tsx
  - apps/web/src/app/clusters/[id]/events/page.tsx
  - apps/web/src/app/clusters/[id]/page.tsx
  - apps/web/src/app/clusters/[id]/helm/page.tsx
  - apps/web/src/app/clusters/[id]/network-policies/page.tsx
  - apps/web/src/app/clusters/[id]/autoscaling/page.tsx
  - apps/web/src/app/clusters/[id]/metrics/page.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/clusters/page.tsx
  - apps/web/src/app/health/page.tsx
  - apps/web/src/app/events/page.tsx
  - apps/web/src/app/deployments/page.tsx
  - apps/web/src/app/namespaces/page.tsx
  - apps/web/src/app/services/page.tsx
  - apps/web/src/app/logs/page.tsx
  - apps/web/src/components/Sidebar.tsx
  - apps/web/src/components/TopBar.tsx
  - apps/web/src/components/ClusterHealthIndicator.tsx
  - apps/web/src/components/NotificationsPanel.tsx
  - apps/web/src/components/topology/TopologyMap.tsx
  - apps/web/src/components/network/NetworkPolicyGraph.tsx
  - apps/web/src/components/crds/CrdBrowser.tsx
  - apps/web/src/components/metrics/NodeMetricsTable.tsx
  - apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx
  - apps/web/src/components/metrics/ResourceSparkline.tsx
  - apps/web/src/components/widgets/ClusterHealthWidget.tsx
  - apps/web/src/components/widgets/StatCardsWidget.tsx
  - apps/web/src/components/widgets/PodStatusWidget.tsx
  - apps/web/src/components/widgets/DeploymentListWidget.tsx
autonomous: true
requirements:
  - D-08
  - D-09

must_haves:
  truths:
    - "Zero refetchInterval for watched K8s resources across entire frontend"
    - "refetchInterval kept for DB queries (users, teams, clusters.list, alerts, anomalies, presence) and metrics"
    - "health-sync, node-sync, event-sync jobs removed from server.ts startup"
    - "health-sync.ts, node-sync.ts, event-sync.ts files deleted"
    - "WatchManager initialized in server.ts startup sequence"
    - "watch-db-writer started in server.ts startup and stopped on shutdown"
    - "Old watch managers (ClusterWatchManager, ResourceWatchManager) removed from server.ts"
  artifacts:
    - path: "apps/api/src/server.ts"
      provides: "Updated server startup with WatchManager + watch-db-writer, without 3 removed jobs"
      contains: "watchManager"
    - path: "apps/api/src/lib/cache-keys.ts"
      provides: "Slimmed cache keys without watched resource keys (except those used by kept procedures)"
  key_links:
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/lib/watch-manager.ts"
      via: "WatchManager initialization on startup"
      pattern: "watchManager"
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/lib/watch-db-writer.ts"
      via: "startWatchDbWriter() on startup, stopWatchDbWriter() on shutdown"
      pattern: "watchDbWriter"
---

<phase_rules>
## Phase 10 — Mandatory Rules (apply to ALL plans)

1. **Use `ui-ux-pro-max` and `frontend-design` skills** for all frontend/design tasks.
2. **No hardcoded values.** All constants MUST go in centralized config files. Zero magic numbers in implementation files.
3. **QA validation after each wave.** After all plans in a wave complete, run QA with Playwright browser — screenshots, console errors, functionality verification.
</phase_rules>

<objective>
Big bang switch (D-08): Remove ALL refetchInterval from watched resources across the entire frontend (~45 entries). Remove 3 background sync jobs (D-09). Wire WatchManager and watch-db-writer into server.ts. Clean up old watch managers and cache keys.

Purpose: This is the final integration step that activates the Lens-style live data pipeline. After this plan, the app runs on watch-based real-time data instead of polling.

Output: Zero polling for watched resources, 3 jobs removed, WatchManager + watch-db-writer in server startup, old code cleaned up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-CONTEXT.md
@.planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-RESEARCH.md
@.planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-01-SUMMARY.md
@.planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-02-SUMMARY.md
@.planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-03-SUMMARY.md
@.planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server.ts integration — WatchManager + watch-db-writer init, remove 3 jobs, clean up old watch managers</name>
  <files>
    apps/api/src/server.ts,
    apps/api/src/jobs/health-sync.ts,
    apps/api/src/jobs/node-sync.ts,
    apps/api/src/jobs/event-sync.ts,
    apps/api/src/lib/cache-keys.ts
  </files>
  <read_first>
    apps/api/src/server.ts,
    apps/api/src/jobs/health-sync.ts,
    apps/api/src/jobs/node-sync.ts,
    apps/api/src/jobs/event-sync.ts,
    apps/api/src/lib/watch-manager.ts,
    apps/api/src/lib/watch-db-writer.ts,
    apps/api/src/lib/cluster-watch-manager.ts,
    apps/api/src/lib/resource-watch-manager.ts,
    apps/api/src/lib/cache-keys.ts,
    apps/api/src/config/jobs.ts,
    apps/api/src/routers/pods.ts
  </read_first>
  <action>
    **Step 1: Update server.ts**

    1. Add imports:
       - `import { watchManager } from './lib/watch-manager.js'`
       - `import { startWatchDbWriter, stopWatchDbWriter } from './lib/watch-db-writer.js'`
    2. Remove imports for health-sync, node-sync, event-sync jobs
    3. Remove imports for ClusterWatchManager and ResourceWatchManager (if imported)
    4. In the startup sequence (after DB and Redis are ready), add:
       ```typescript
       // Initialize WatchManager (Lens-style live data pipeline)
       // Watches are started on-demand when SSE clients subscribe
       console.log('[server] WatchManager ready (watches start on first subscriber)')

       // Start watch-db-writer — persists watch data to PostgreSQL (replaces health-sync, node-sync, event-sync)
       startWatchDbWriter()
       console.log('[server] WatchDbWriter started')
       ```
       Note: WatchManager doesn't need explicit init — it's a singleton that starts watches on first `subscribe()`.
    5. Remove the startup calls for health-sync, node-sync, event-sync jobs. Find lines like:
       ```typescript
       // REMOVE these:
       startHealthSync()
       startNodeSync()
       startEventSync()
       ```
    6. In the graceful shutdown handler (SIGTERM/SIGINT), add:
       - `watchManager.stopAll()`
       - `stopWatchDbWriter()`
    7. Remove any references to ClusterWatchManager or ResourceWatchManager startup/stop
    8. Keep: metrics-history-collector, metrics-stream-job, alert-evaluator, deploy-smoke-test

    **Step 2: Delete 3 job files**

    Delete these files entirely:
    - `apps/api/src/jobs/health-sync.ts`
    - `apps/api/src/jobs/node-sync.ts`
    - `apps/api/src/jobs/event-sync.ts`

    **Step 3: Slim down cache-keys.ts**

    Read `apps/api/src/lib/cache-keys.ts`. Remove cache key builders for watched resources that are no longer cached in Redis.

    **CRITICAL: Before removing any cache key, grep the codebase to verify it is NOT referenced by a kept procedure.** Specifically:
    - `k8sPodsStored` is used by `pods.listStored` procedure (line 162 in pods.ts) and `pods.delete` invalidation (line 222) — **KEEP k8sPodsStored**
    - `k8sPodMetrics` is used for metrics caching — **KEEP**
    - `k8sNodeMetrics` is used for metrics caching — **KEEP**
    - `k8sVersion` is used by clusters router — **KEEP**
    - Keys for helm, crds, rbac, network-policies, resource-quotas, yaml — **KEEP all**
    - `k8sPrefix` — **KEEP** (used by SCAN invalidation)
    - `K8S_GLOBAL_PREFIX` — **KEEP**
    - `k8sTopology` — can REMOVE (topology now reads from WatchManager)
    - `k8sDeploymentsListGlobal` — can REMOVE (deployments.listAll now reads from WatchManager)

    Remove only these (verified no kept procedure references):
    - `k8sPods` (pods.list now reads from WatchManager; but check pods.delete invalidation — if it still invalidates this key, keep it as a no-op is harmless)
    - `k8sNodes` (nodes.listLive reads from WatchManager)
    - `k8sDeployments` (deployments.listDetail reads from WatchManager)
    - `k8sDeploymentsList` (deployments list reads from WatchManager)
    - `k8sServices` (services.list reads from WatchManager)
    - `k8sNamespaces` (namespaces.list reads from WatchManager)
    - `k8sEvents` (events.list reads from WatchManager)
    - `k8sTopology` (topology reads from WatchManager)
    - `k8sDeploymentsListGlobal` (deployments.listAll reads from WatchManager)

    **KEEP `k8sPodsStored`** — it is still actively used by the `pods.listStored` procedure which was explicitly kept unchanged in Plan 04.

    Run `grep -rn 'CACHE_KEYS\.' apps/api/src/ | grep -v node_modules` after changes to verify no dangling references.

    **Step 4: Update config/jobs.ts**

    Remove interval constants for the 3 deleted jobs:
    - Remove `HEALTH_SYNC_MS` (or equivalent)
    - Remove `NODE_SYNC_MS`
    - Remove `EVENT_SYNC_MS`
    - Keep all other job intervals
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm build && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'health-sync\|node-sync\|event-sync' apps/api/src/server.ts` returns NO matches
    - `grep 'watchManager' apps/api/src/server.ts` returns match
    - `grep 'startWatchDbWriter' apps/api/src/server.ts` returns match
    - `grep 'stopWatchDbWriter' apps/api/src/server.ts` returns match
    - `grep 'stopAll' apps/api/src/server.ts` returns match (shutdown handler)
    - `ls apps/api/src/jobs/health-sync.ts 2>/dev/null` returns nothing (file deleted)
    - `ls apps/api/src/jobs/node-sync.ts 2>/dev/null` returns nothing (file deleted)
    - `ls apps/api/src/jobs/event-sync.ts 2>/dev/null` returns nothing (file deleted)
    - `grep 'k8sPodsStored' apps/api/src/lib/cache-keys.ts` returns match (KEPT — used by pods.listStored)
    - `grep 'metrics-history-collector\|metricsHistoryCollector' apps/api/src/server.ts` returns match (kept)
    - `pnpm build` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    Server.ts initializes WatchManager, starts watch-db-writer, stops both on shutdown. 3 sync jobs deleted. Cache keys slimmed (k8sPodsStored kept for listStored). Remaining jobs (metrics-collector, metrics-stream, alert-evaluator, deploy-smoke) untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove refetchInterval from cluster-specific pages (~20 files in apps/web/src/app/clusters/[id]/)</name>
  <files>
    apps/web/src/app/clusters/[id]/pods/page.tsx,
    apps/web/src/app/clusters/[id]/deployments/page.tsx,
    apps/web/src/app/clusters/[id]/services/page.tsx,
    apps/web/src/app/clusters/[id]/ingresses/page.tsx,
    apps/web/src/app/clusters/[id]/statefulsets/page.tsx,
    apps/web/src/app/clusters/[id]/daemonsets/page.tsx,
    apps/web/src/app/clusters/[id]/jobs/page.tsx,
    apps/web/src/app/clusters/[id]/cronjobs/page.tsx,
    apps/web/src/app/clusters/[id]/hpa/page.tsx,
    apps/web/src/app/clusters/[id]/configmaps/page.tsx,
    apps/web/src/app/clusters/[id]/secrets/page.tsx,
    apps/web/src/app/clusters/[id]/pvcs/page.tsx,
    apps/web/src/app/clusters/[id]/namespaces/page.tsx,
    apps/web/src/app/clusters/[id]/nodes/page.tsx,
    apps/web/src/app/clusters/[id]/events/page.tsx,
    apps/web/src/app/clusters/[id]/page.tsx,
    apps/web/src/app/clusters/[id]/helm/page.tsx,
    apps/web/src/app/clusters/[id]/network-policies/page.tsx,
    apps/web/src/app/clusters/[id]/autoscaling/page.tsx,
    apps/web/src/app/clusters/[id]/metrics/page.tsx
  </files>
  <read_first>
    apps/web/src/app/clusters/[id]/pods/page.tsx,
    apps/web/src/app/clusters/[id]/deployments/page.tsx,
    apps/web/src/app/clusters/[id]/page.tsx,
    apps/web/src/app/clusters/[id]/nodes/page.tsx,
    apps/web/src/app/clusters/[id]/metrics/page.tsx,
    .planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-RESEARCH.md
  </read_first>
  <action>
    Remove `refetchInterval` from ALL watched K8s resource queries in cluster-specific pages.

    **Step 1: Run inventory grep to confirm current state**
    ```bash
    grep -rn 'refetchInterval' apps/web/src/app/clusters/[id]/ | grep -v node_modules
    ```

    **Step 2: For each file listed above:**
    - Open the file
    - Find every `useQuery` or `trpc.xxx.useQuery` call with `refetchInterval`
    - Classify each as REMOVE (K8s resource query) or KEEP (metrics/DB query)
    - Remove the `refetchInterval: <number>` option from K8s resource queries
    - If `refetchInterval` was the only option in the config object, clean up the empty config
    - Do NOT remove the query itself — just the polling interval

    **Removal rules for cluster pages:**
    - `clusters.live` queries: REMOVE (SSE covers live data)
    - `pods.list` queries: REMOVE
    - `deployments.listDetail` queries: REMOVE
    - `services.list` / `services.listDetail` queries: REMOVE
    - `nodes.listLive` queries: REMOVE
    - `events.list` queries: REMOVE
    - All other K8s resource list queries: REMOVE
    - `topology.get` queries: REMOVE
    - `networkPolicies.list` in network-policies page: REMOVE
    - `crds.list` queries: REMOVE
    - `helm.list` queries: REMOVE (helm reads from K8s secrets, now via WatchManager fallback)

    **KEEP rules (do not touch these):**
    - `metrics.history` in metrics/page.tsx: KEEP (Metrics API not watchable)
    - Any DB-only queries if present

    **Step 3: Verify cluster pages**
    ```bash
    grep -rn 'refetchInterval' apps/web/src/app/clusters/\[id\]/ | grep -v node_modules | wc -l
    ```
    Should return 0 or very low (only metrics queries kept).
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm build && pnpm typecheck && echo "Cluster pages refetchInterval count:" && grep -rn 'refetchInterval' apps/web/src/app/clusters/\[id\]/ | grep -v node_modules | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'refetchInterval' apps/web/src/app/clusters/[id]/pods/page.tsx` returns NO matches
    - `grep -rn 'refetchInterval' apps/web/src/app/clusters/[id]/deployments/page.tsx` returns NO matches
    - `grep -rn 'refetchInterval' apps/web/src/app/clusters/[id]/services/page.tsx` returns NO matches
    - `grep -rn 'refetchInterval' apps/web/src/app/clusters/[id]/nodes/page.tsx` returns NO matches
    - `grep -rn 'refetchInterval' apps/web/src/app/clusters/[id]/ | grep -v node_modules | wc -l` returns approximately 0-2 (only metrics if any)
    - `pnpm build` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    All cluster-specific pages have zero refetchInterval for watched K8s resources. Metrics queries kept if present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove refetchInterval from shared components and global pages (~23 files)</name>
  <files>
    apps/web/src/app/page.tsx,
    apps/web/src/app/clusters/page.tsx,
    apps/web/src/app/health/page.tsx,
    apps/web/src/app/events/page.tsx,
    apps/web/src/app/deployments/page.tsx,
    apps/web/src/app/namespaces/page.tsx,
    apps/web/src/app/services/page.tsx,
    apps/web/src/app/logs/page.tsx,
    apps/web/src/components/Sidebar.tsx,
    apps/web/src/components/TopBar.tsx,
    apps/web/src/components/ClusterHealthIndicator.tsx,
    apps/web/src/components/NotificationsPanel.tsx,
    apps/web/src/components/topology/TopologyMap.tsx,
    apps/web/src/components/network/NetworkPolicyGraph.tsx,
    apps/web/src/components/crds/CrdBrowser.tsx,
    apps/web/src/components/metrics/NodeMetricsTable.tsx,
    apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx,
    apps/web/src/components/metrics/ResourceSparkline.tsx,
    apps/web/src/components/widgets/ClusterHealthWidget.tsx,
    apps/web/src/components/widgets/StatCardsWidget.tsx,
    apps/web/src/components/widgets/PodStatusWidget.tsx,
    apps/web/src/components/widgets/DeploymentListWidget.tsx
  </files>
  <read_first>
    apps/web/src/app/page.tsx,
    apps/web/src/components/Sidebar.tsx,
    apps/web/src/components/TopBar.tsx,
    apps/web/src/components/widgets/ClusterHealthWidget.tsx,
    apps/web/src/components/widgets/StatCardsWidget.tsx,
    apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx,
    apps/web/src/components/metrics/ResourceSparkline.tsx,
    .planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-RESEARCH.md
  </read_first>
  <action>
    Remove `refetchInterval` from watched K8s resource queries in shared components and global pages.

    **Step 1: Run inventory grep**
    ```bash
    grep -rn 'refetchInterval' apps/web/src/ --include='*.tsx' --include='*.ts' | grep -v node_modules | grep -v 'app/clusters/\[id\]'
    ```

    **Step 2: REMOVE these refetchInterval entries**

    For each file listed, find and remove `refetchInterval` from K8s resource queries:
    - `health.status` queries: REMOVE (WatchManager provides health from watch data)
    - `deployments.listAll` queries: REMOVE
    - `events.list` queries on global pages: REMOVE
    - `pods.list` queries in widgets: REMOVE
    - `topology.get` queries: REMOVE
    - `networkPolicies.list` in NetworkPolicyGraph: REMOVE
    - `crds.list` in CrdBrowser: REMOVE
    - Any K8s resource list query with refetchInterval: REMOVE

    **Step 3: KEEP these refetchInterval entries (do NOT touch)**

    - `clusters.listWithHealth` in Sidebar/health pages (DB query): KEEP
    - `clusters.list` in Sidebar/TopBar (DB query): KEEP
    - `users.list` in settings: KEEP
    - `teams.list` in settings: KEEP
    - `alerts.list` in AlertFeedWidget: KEEP
    - `anomalies.listAll` in AnomalyTimeline: KEEP
    - `presence.list` in usePresence: KEEP
    - `metrics.history` in MetricsTimeSeriesPanel/ResourceSparkline/NodeMetricsTable (Metrics API not watchable): KEEP

    **Step 4: Verify final global count**
    ```bash
    grep -rn 'refetchInterval' apps/web/src/ | grep -v node_modules | wc -l
    ```
    Should return approximately 19 (DB + metrics queries only).
  </action>
  <verify>
    <automated>cd /Users/viktork/Documents/private/GitHub-private/voyager-platform && pnpm build && pnpm typecheck && echo "Total remaining refetchInterval:" && grep -rn 'refetchInterval' apps/web/src/ | grep -v node_modules | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'refetchInterval' apps/web/src/components/Sidebar.tsx` returns 1 (only clusters.list DB query kept)
    - `grep -rn 'refetchInterval' apps/web/src/ | grep -v node_modules | wc -l` returns approximately 19 or fewer
    - `grep 'refetchInterval' apps/web/src/app/settings/page.tsx` returns matches (kept: users, teams)
    - `grep 'refetchInterval' apps/web/src/hooks/usePresence.ts` returns match (kept: not K8s)
    - `grep 'refetchInterval' apps/web/src/components/metrics/MetricsTimeSeriesPanel.tsx` returns match (kept: metrics)
    - `pnpm build` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    Big bang switch complete (D-08). ~45 refetchInterval entries removed for watched K8s resources across both cluster pages and shared components. ~19 kept for DB queries and metrics. App runs on watch-based live data.
  </done>
</task>

</tasks>

<verification>
- `pnpm build && pnpm typecheck` passes
- `pnpm test` passes (all existing tests still green)
- `grep -rn 'refetchInterval' apps/web/src/ | grep -v node_modules | wc -l` returns ~19
- `grep 'health-sync\|node-sync\|event-sync' apps/api/src/server.ts` returns 0 matches
- `ls apps/api/src/jobs/health-sync.ts apps/api/src/jobs/node-sync.ts apps/api/src/jobs/event-sync.ts 2>&1 | grep -c 'No such file'` returns 3
- `grep 'k8sPodsStored' apps/api/src/lib/cache-keys.ts` returns match (kept for pods.listStored)
- `grep 'startWatchDbWriter' apps/api/src/server.ts` returns match
</verification>

<success_criteria>
Big bang switch (D-08) complete: zero refetchInterval for watched resources, ~19 kept for DB/metrics. Three sync jobs removed (D-09). WatchManager + watch-db-writer integrated into server startup/shutdown. Cache keys slimmed without breaking kept procedures. Full test suite passes. The app now runs on Lens-style live data — K8s Watch events carry transformed objects to the browser via SSE, applied directly to TanStack Query cache.
</success_criteria>

<qa_gate>
## Wave 3 QA Validation — FINAL PHASE QA (after Plan 05 completes)

**This is the final wave.** Full QA sweep required before declaring phase complete:

**MANDATORY: Kill and restart dev servers completely.** Verify API logs show `[server] WatchManager ready` and NO mentions of health-sync/node-sync/event-sync starting.

### Build Gate
- `pnpm build && pnpm typecheck && pnpm test` — ALL must pass

### Functional Tests — Big Bang Switch Verification

**Test 1: Sync jobs are gone**
- Check API startup logs: `health-sync`, `node-sync`, `event-sync` must NOT appear
- `ls apps/api/src/jobs/health-sync.ts apps/api/src/jobs/node-sync.ts apps/api/src/jobs/event-sync.ts 2>&1` — all must return "No such file"
- **FAIL if:** any of the 3 deleted job files still exist or appear in startup logs

**Test 2: refetchInterval removed from watched resources**
- `grep -rn 'refetchInterval' apps/web/src/ | grep -v node_modules | wc -l` — should return ~19-24 (only metrics, presence, alerts, anomalies, settings, health.status)
- `grep -rn 'refetchInterval' apps/web/src/app/clusters/\[id\]/ | grep -v metrics` — should return 0
- **FAIL if:** any cluster resource page (pods, deployments, services, etc.) still has refetchInterval

**Test 3: Full SSE pipeline end-to-end (repeat Wave 2 tests)**
- SSE sends `event: watch` with full objects — verify in browser
- setQueryData being used — no periodic tRPC refetch calls after initial load
- ConnectionStatusBadge shows "Live"
- **FAIL if:** any of these don't work (same criteria as Wave 2 functional tests)

**Test 4: Data accuracy — compare ALL resource types against cluster**
- Pods: UI count vs `kubectl get pods -A | tail -n+2 | wc -l`
- Deployments: UI count vs `kubectl get deployments -A | tail -n+2 | wc -l`
- Services: UI count vs `kubectl get services -A | tail -n+2 | wc -l`
- Nodes: UI count vs `kubectl get nodes | tail -n+2 | wc -l`
- Events: UI shows recent events from cluster
- **FAIL if:** any resource count differs by more than 10%

**Test 5: Non-watched features still work (no regressions)**
- Metrics page: charts load with data (uses kept refetchInterval path)
- Helm page: releases load
- CRDs page: CRD list loads
- RBAC page: matrix renders
- Login page: clear cookies, verify form, log in successfully
- **FAIL if:** any non-watched feature is broken

**Test 6: Live data verification**
- If possible, trigger a real change in the cluster (e.g., `kubectl scale deployment <name> --replicas=X`)
- Verify UI updates within ~2 seconds WITHOUT manual page refresh
- **FAIL if:** UI requires manual refresh to see changes (means SSE pipeline not working)

**Test 7: Theme consistency**
- Test dark + light mode on: dashboard, cluster overview, pods page, events page
- **FAIL if:** mixed theme elements or broken styling

**Test 8: Console errors**
- Check console on every page visited during tests
- ZERO `[ERROR]` entries across all pages
- Initial SSE reconnect WARNINGs acceptable only if they resolve within 5 seconds

### Failure Protocol
If ANY test fails:
1. Diagnose root cause
2. Fix
3. Restart dev servers
4. Re-run ALL functional tests from Test 1
5. Repeat until ALL pass
6. Phase is NOT complete until every test passes
</qa_gate>

<output>
After completion, create `.planning/phases/10-lens-style-live-data-k8s-watch-stream-architecture/10-05-SUMMARY.md`
</output>
