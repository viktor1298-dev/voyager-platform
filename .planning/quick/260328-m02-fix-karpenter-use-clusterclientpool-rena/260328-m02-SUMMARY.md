---
phase: quick
plan: 260328-m02
subsystem: api
tags: [karpenter, kubernetes, cluster-client-pool, refactor]

requires: []
provides:
  - KarpenterService using ClusterClientPool for encrypted credential support
  - Karpenter tab label rename in cluster detail UI
affects: [karpenter, autoscaling, cluster-detail]

tech-stack:
  added: []
  patterns:
    - "KarpenterService uses injected kubeConfigGetter function backed by ClusterClientPool singleton"

key-files:
  created: []
  modified:
    - apps/api/src/lib/karpenter-service.ts
    - apps/api/src/__tests__/karpenter-service.test.ts
    - apps/web/src/app/clusters/[id]/layout.tsx
    - apps/web/src/app/clusters/[id]/autoscaling/page.tsx

key-decisions:
  - "Inject kubeConfigGetter function instead of ClusterClientPool directly for testability"

patterns-established:
  - "KarpenterService dependency injection: constructor takes kubeConfigGetter function, factory wires in clusterClientPool.getClient"

requirements-completed: []

duration: 4min
completed: 2026-03-28
---

# Quick Task 260328-m02: Fix Karpenter - Use ClusterClientPool + Rename Tab Summary

**KarpenterService refactored from duplicated cluster connection logic to ClusterClientPool injection, enabling encrypted credential support; tab label renamed from "Autoscaling" to "Karpenter"**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-28T12:53:27Z
- **Completed:** 2026-03-28T12:57:45Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- KarpenterService now uses ClusterClientPool (via injected kubeConfigGetter) instead of reimplementing cluster lookup that failed on encrypted credentials
- Removed ~25 lines of duplicated connection logic (getClusterKubeConfig method, clusterSchema, unused imports)
- Tab label in cluster detail 10-tab bar reads "Karpenter" instead of "Autoscaling"
- Page title reads "Karpenter" instead of "Cluster Autoscaling"

## Task Commits

Each task was committed atomically:

1. **Task 1: Refactor KarpenterService to use ClusterClientPool and update tests** - `ba3db28` (fix)
2. **Task 2: Rename tab label from "Autoscaling" to "Karpenter" and update page title** - `e2798df` (feat)

## Files Created/Modified
- `apps/api/src/lib/karpenter-service.ts` - Replaced getClusterKubeConfig with injected kubeConfigGetter, removed unused imports (zod, connectionConfigSchema, createKubeConfigForCluster, clusters table)
- `apps/api/src/__tests__/karpenter-service.test.ts` - Updated tests to mock kubeConfigGetter instead of DB select chain; added mockCacheDb for cache operations
- `apps/web/src/app/clusters/[id]/layout.tsx` - Changed autoscaling tab label from "Autoscaling" to "Karpenter"
- `apps/web/src/app/clusters/[id]/autoscaling/page.tsx` - Changed page title from "Cluster Autoscaling" to "Karpenter"

## Decisions Made
- Injected kubeConfigGetter as a function `(clusterId: string) => Promise<KubeConfig>` rather than passing the ClusterClientPool instance directly, keeping the service testable with simple mock functions

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Known Stubs
None.

## Self-Check: PASSED

All 4 modified files verified present. Both task commits (ba3db28, e2798df) verified in git log.
