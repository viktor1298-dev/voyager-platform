# Flaky Test Quarantine

Tests here are known to fail inconsistently. They run as WARNING not BLOCKER.

## Process
1. Test fails >2x inconsistently → move here
2. Fix root cause → move back to main suite
3. If unfixable → convert to monitoring test (track trend)

## Current Quarantined Tests
(empty — add as discovered)

## Flaky Rate Tracking
See `test-metrics.json` in this directory.
